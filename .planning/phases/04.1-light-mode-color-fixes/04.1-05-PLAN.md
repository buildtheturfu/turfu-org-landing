---
phase: 04.1-light-mode-color-fixes
plan: 05
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - src/components/admin/AdminDashboard.tsx
  - src/components/admin/ArticleEditor.tsx
  - src/app/[locale]/admin/login/page.tsx
  - src/components/LanguageSwitcher.tsx
autonomous: true

must_haves:
  truths:
    - "Admin dashboard readable in light mode"
    - "Article editor forms visible with proper input styling"
    - "Admin login page usable in light mode"
    - "Language switcher dropdown visible in both modes"
  artifacts:
    - path: "src/components/admin/AdminDashboard.tsx"
      provides: "Theme-aware admin dashboard"
      contains: "text-foreground"
    - path: "src/components/LanguageSwitcher.tsx"
      provides: "Theme-aware language dropdown"
      contains: "bg-overlay"
  key_links:
    - from: "admin/*.tsx"
      to: "globals.css"
      via: "semantic color classes"
      pattern: "(text-foreground|bg-surface|border-border)"
---

<objective>
Replace hardcoded dark-mode colors in admin components and LanguageSwitcher.

Purpose: Admin pages are internal but still need to be usable in light mode. LanguageSwitcher appears on every page and must be visible.
Output: Four remaining components using semantic color tokens.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-light-mode-color-fixes/04.1-RESEARCH.md
@src/components/admin/AdminDashboard.tsx
@src/components/admin/ArticleEditor.tsx
@src/app/[locale]/admin/login/page.tsx
@src/components/LanguageSwitcher.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Admin components</name>
  <files>src/components/admin/AdminDashboard.tsx, src/components/admin/ArticleEditor.tsx, src/app/[locale]/admin/login/page.tsx</files>
  <action>
Apply the standard replacement pattern to all admin files:

**Text colors:**
- `text-white` → `text-foreground`
- `text-turfu-muted` → `text-foreground-muted`
- `hover:text-white` → `hover:text-foreground`
- `placeholder:text-turfu-muted` → `placeholder:text-foreground-muted`

**Backgrounds:**
- `bg-turfu-dark` → `bg-surface`
- `bg-turfu-darker` → `bg-surface-muted`
- `bg-white/5` → `bg-overlay`
- `bg-white/10` → `bg-overlay-hover`

**Borders:**
- `border-white/5` → `border-border-muted`
- `border-white/10` → `border-border`
- `border-white/20` → `border-border`

**Form inputs specifically:**
- Input backgrounds: `bg-overlay` with `border-border`
- Input text: `text-foreground`
- Placeholder text: `placeholder:text-foreground-muted`
- Focus states: keep existing ring patterns but ensure ring-offset uses `var(--background)`

**AdminDashboard.tsx:** May have stats cards, tables, action buttons
**ArticleEditor.tsx:** May have form fields, text areas, markdown preview
**admin/login/page.tsx:** Login form with email/password inputs
  </action>
  <verify>
Run `grep -E "text-white|bg-white/|border-white|turfu-dark" src/components/admin/AdminDashboard.tsx src/components/admin/ArticleEditor.tsx src/app/[locale]/admin/login/page.tsx` - should return no results.
  </verify>
  <done>
- Admin dashboard cards and tables visible in light mode
- Article editor form inputs styled correctly
- Login page readable with visible form fields
  </done>
</task>

<task type="auto">
  <name>Task 2: Update LanguageSwitcher component</name>
  <files>src/components/LanguageSwitcher.tsx</files>
  <action>
**LanguageSwitcher.tsx replacements:**
- `text-white` → `text-foreground`
- `bg-white/10` → `bg-overlay-hover`
- `hover:bg-white/20` → `hover:bg-overlay` (adjust as needed for visible hover)
- `border-white/10` → `border-border`
- Dropdown background: `bg-surface` or `bg-surface-elevated`
- Dropdown items hover: `hover:bg-overlay`

If using a select element or custom dropdown:
- Ensure dropdown menu background contrasts with page
- Ensure selected state is visible
- Ensure hover states work in both modes

This component appears in the Navbar, so it must be visible against the Navbar background in both light and dark modes.
  </action>
  <verify>
Run `grep -E "text-white|bg-white/|border-white" src/components/LanguageSwitcher.tsx` - should return no results.
Run `grep -E "text-foreground|bg-overlay|bg-surface" src/components/LanguageSwitcher.tsx` - should find semantic classes.
  </verify>
  <done>
- LanguageSwitcher button visible in both modes
- Dropdown menu has proper background contrast
- Hover and selected states work correctly
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run build` succeeds
2. Admin pages functional in light mode
3. Language switcher visible and usable in navbar
</verification>

<success_criteria>
- All admin components use semantic color tokens
- LanguageSwitcher works in both light and dark modes
- No remaining hardcoded dark-mode colors in these files
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-light-mode-color-fixes/04.1-05-SUMMARY.md`
</output>
