---
phase: 04.1-light-mode-color-fixes
plan: 06
type: execute
wave: 3
depends_on: ["04.1-02", "04.1-03", "04.1-04", "04.1-05"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All pages readable in light mode without white-on-white text"
    - "All surfaces have visible borders/backgrounds in light mode"
    - "All interactive elements have visible hover states"
    - "Dark mode appearance unchanged from before migration"
  artifacts: []
  key_links: []
---

<objective>
Final verification of light mode color fixes across the entire application.

Purpose: Ensure all color fixes work correctly in both light and dark modes. This is a visual verification checkpoint - no code changes expected.
Output: Confirmed working light mode with WCAG AA compliant colors.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-light-mode-color-fixes/04.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no remaining hardcoded colors</name>
  <files></files>
  <action>
Run comprehensive grep to find any remaining hardcoded dark-mode colors:

```bash
# Check for text-white outside of gradient/accent contexts
grep -rn "text-white" src/components src/app --include="*.tsx" | grep -v "btn-primary" | grep -v "gradient"

# Check for bg-white/ patterns (should be zero outside backdrops)
grep -rn "bg-white/" src/components src/app --include="*.tsx"

# Check for border-white patterns
grep -rn "border-white" src/components src/app --include="*.tsx"

# Check for turfu-dark/darker (should only be in config or specific cases)
grep -rn "turfu-dark" src/components src/app --include="*.tsx"

# Check for turfu-muted (should be replaced with foreground-muted)
grep -rn "turfu-muted" src/components src/app --include="*.tsx"
```

If any results found, document which files still need attention. Minor remaining instances in legitimate contexts (gradient buttons, backdrops) are acceptable.
  </action>
  <verify>
Grep commands return minimal/no results for hardcoded colors in component files.
  </verify>
  <done>
- Comprehensive search confirms semantic tokens used throughout
- Any legitimate exceptions documented
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete light mode color system across 26 files:
- CSS variables for surfaces, text, borders, overlays
- Tailwind semantic color mappings
- Layout components (Navbar, Footer, ThemeToggle, BackToTop)
- Landing page sections (Hero, Problem, CTA, Ecosystem, Vision, Architecture)
- Content module (Sidebar, Drawer, Cards, TOC, MarkdownRenderer, pages)
- Admin pages and LanguageSwitcher
  </what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Open http://localhost:3000 in browser

**Test Light Mode:**
3. Click theme toggle to switch to light mode
4. Verify homepage:
   - Navbar text and links visible
   - Hero section text readable
   - All section cards have visible borders
   - Footer links readable
   - Theme toggle and BackToTop buttons visible

5. Navigate to /content:
   - Sidebar navigation readable
   - Article cards have visible backgrounds
   - On mobile: test hamburger menu - drawer should be visible

6. Open an article:
   - Markdown prose readable
   - Table of contents links visible
   - Code blocks have appropriate contrast

7. Check admin login page (/admin/login):
   - Form fields visible with proper borders
   - Button text readable

**Test Dark Mode Regression:**
8. Toggle back to dark mode
9. Verify all above elements still look correct (no regressions)

**Expected:** All text readable, all surfaces have appropriate contrast, no white-on-white or invisible elements.
  </how-to-verify>
  <resume-signal>Type "approved" if light mode works correctly, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
Phase 4.1 complete when:
1. User confirms light mode visual verification passes
2. Dark mode regression check passes
3. All 26 files migrated to semantic color tokens
</verification>

<success_criteria>
- All text readable in light mode (no white-on-white)
- All surfaces have appropriate contrast in both modes
- Icons and borders visible in light mode
- Dark mode appearance unchanged
- User visual verification approved
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-light-mode-color-fixes/04.1-06-SUMMARY.md`
</output>
