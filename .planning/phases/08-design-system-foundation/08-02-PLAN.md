---
phase: 08-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/Navbar.tsx
  - src/components/Footer.tsx
  - src/components/ThemeToggle.tsx
  - src/components/BackToTop.tsx
  - src/components/LanguageSwitcher.tsx
  - src/components/sections/Hero.tsx
  - src/components/sections/Vision.tsx
  - src/components/sections/Problem.tsx
  - src/components/sections/Principles.tsx
  - src/components/sections/Architecture.tsx
  - src/components/sections/CTA.tsx
  - src/components/sections/Ecosystem.tsx
  - src/components/content/TableOfContents.tsx
  - src/components/content/MarkdownRenderer.tsx
  - src/components/content/MobileSidebarDrawer.tsx
  - src/components/content/ArticleCard.tsx
  - src/components/content/ContentSidebar.tsx
  - src/components/admin/ArticleEditor.tsx
  - src/components/admin/TagInput.tsx
  - src/components/admin/ComboboxInput.tsx
  - src/components/admin/AdminDashboard.tsx
  - src/components/admin/SaveIndicator.tsx
  - src/app/[locale]/admin/login/page.tsx
  - src/app/[locale]/content/page.tsx
  - src/app/[locale]/content/layout.tsx
  - src/app/[locale]/content/loading.tsx
  - src/app/[locale]/content/[slug]/page.tsx
  - src/app/[locale]/content/[slug]/loading.tsx
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-05

must_haves:
  truths:
    - "No component uses the old zinc/gray token classes (foreground, surface, turfu-accent, overlay, turfu-dark, turfu-darker, turfu-muted)"
    - "All existing pages render with stone palette colors in both light and dark mode"
    - "H1 and H2 elements use font-display class (Instrument Serif)"
    - "Dark mode toggle works with no flash, no invisible text, no broken colors"
  artifacts:
    - path: "src/components/Navbar.tsx"
      provides: "Navigation bar using new design tokens"
      contains: "text-ink"
    - path: "src/components/content/MarkdownRenderer.tsx"
      provides: "Markdown rendering using new design tokens"
      contains: "text-ink"
  key_links:
    - from: "all component files"
      to: "tailwind.config.ts"
      via: "Tailwind utility classes referencing new color tokens"
      pattern: "text-ink|bg-paper|text-accent|border-border"
    - from: "all component files"
      to: "src/app/globals.css"
      via: "CSS variables resolved at runtime"
      pattern: "var\\(--ink\\)|var\\(--paper\\)"
---

<objective>
Migrate all 28 component and page files from old zinc/gray tokens to new stone palette tokens. Add font-display class to H1/H2 headings. Verify dark mode works end-to-end. Remove all traces of old token system.

Purpose: Complete the design system migration so every page renders with the new stone palette, three-font system, and layer-coded accent colors.
Output: All components using new tokens, zero references to old tokens, dark mode verified.
</objective>

<execution_context>
@/Users/ekitcho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ekitcho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-system-foundation/08-01-SUMMARY.md

<interfaces>
<!-- Token mapping (old -> new) — THE complete replacement table -->
<!-- Every old class on the left MUST become the new class on the right -->

Tailwind class replacements:
  text-foreground         -> text-ink
  text-foreground-muted   -> text-ink-secondary
  text-foreground-secondary -> text-ink-secondary
  bg-surface              -> bg-paper
  bg-surface-elevated     -> bg-paper-warm
  bg-surface-muted        -> bg-paper-depth
  bg-overlay              -> bg-paper-depth
  bg-overlay-hover        -> bg-paper-warm
  border-border           -> border-border  (same utility name, different underlying value)
  border-border-muted     -> border-border  (consolidate to single border token)
  text-turfu-accent       -> text-accent
  text-turfu-accent2      -> text-accent  (consolidate: no more accent2)
  hover:text-turfu-accent -> hover:text-accent
  hover:text-turfu-accent2 -> hover:text-accent-hover
  bg-turfu-accent         -> bg-accent
  bg-turfu-accent/20      -> bg-accent-light
  ring-turfu-accent       -> ring-accent
  focus:ring-turfu-accent -> focus:ring-accent
  focus:border-turfu-accent -> focus:border-accent
  shadow-turfu-accent     -> shadow-accent
  text-turfu-muted        -> text-ink-tertiary
  bg-turfu-dark           -> bg-paper  (dark mode handled by CSS vars)
  bg-turfu-darker         -> bg-paper  (dark mode handled by CSS vars)
  bg-gradient-turfu       -> bg-accent  (gradient removed, solid accent)
  bg-gradient-turfu-hover -> bg-accent-hover
  btn-primary             -> (inline the styles: bg-accent hover:bg-accent-hover text-white font-medium rounded-lg px-6 py-3)
  btn-secondary           -> (inline: border border-border hover:border-ink-tertiary text-ink font-medium rounded-lg px-6 py-3)
  container-narrow        -> max-w-layout mx-auto px-4 sm:px-6 lg:px-8
  bg-clip-text text-transparent bg-gradient-turfu -> text-accent  (remove gradient-text, use solid accent)

Inline var() replacements:
  var(--background)         -> var(--paper)   OR use bg-paper utility
  var(--foreground)         -> var(--ink)      OR use text-ink utility
  var(--surface)            -> var(--paper)
  var(--surface-elevated)   -> var(--paper-warm)
  var(--surface-muted)      -> var(--paper-depth)
  var(--text-primary)       -> var(--ink)
  var(--text-secondary)     -> var(--ink-secondary)
  var(--text-muted)         -> var(--ink-secondary)
  var(--border)             -> var(--border)   (same name, new value)
  var(--border-muted)       -> var(--border)
  var(--overlay)            -> var(--paper-depth)
  var(--overlay-hover)      -> var(--paper-warm)
  var(--ring)               -> var(--accent)
  rgb(var(--turfu-accent))  -> var(--accent)
  rgb(var(--turfu-accent) / 0.3) -> var(--accent-light)

Font class additions:
  H1 elements: add `font-display text-display-1`
  H2 elements: add `font-display text-display-2`
  H3 elements: add `text-heading-3 font-medium` (DM Sans, already body font)
  H4 elements: add `text-heading-4 font-medium`
  Code/pre elements: add `font-mono text-code`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate layout components (Navbar, Footer, ThemeToggle, BackToTop, LanguageSwitcher)</name>
  <files>src/components/Navbar.tsx, src/components/Footer.tsx, src/components/ThemeToggle.tsx, src/components/BackToTop.tsx, src/components/LanguageSwitcher.tsx</files>
  <read_first>
    - src/components/Navbar.tsx
    - src/components/Footer.tsx
    - src/components/ThemeToggle.tsx
    - src/components/BackToTop.tsx
    - src/components/LanguageSwitcher.tsx
    - tailwind.config.ts (confirm new token names are available)
  </read_first>
  <action>
Apply the token mapping from the interfaces block above to all 5 layout component files. For each file:

1. **Find every old class/var** and replace with the new equivalent per the mapping table
2. **Replace `container-narrow`** with `max-w-layout mx-auto px-4 sm:px-6 lg:px-8`
3. **Replace `bg-gradient-turfu bg-clip-text text-transparent`** with `text-accent` (Footer brand text)
4. **Replace inline `var(--*)` references** per the inline var() mapping table
5. **Replace `btn-primary`/`btn-secondary`** with inlined Tailwind classes using new tokens

Specific per-file notes:

**Navbar.tsx**: Replace any `text-foreground` with `text-ink`, any `bg-surface-*` with `bg-paper-*`, any `turfu-accent` with `accent`. If there is a gradient brand text, replace with `text-accent`.

**Footer.tsx**: Replace `border-[var(--border-muted)]` with `border-border`. Replace `container-narrow` with `max-w-layout mx-auto px-4 sm:px-6 lg:px-8`. Replace `bg-gradient-turfu bg-clip-text text-transparent` with `text-accent`. Replace `text-[var(--foreground-muted)]` with `text-ink-secondary`. Replace `hover:text-[var(--foreground)]` with `hover:text-ink`.

**ThemeToggle.tsx**: Replace `bg-[var(--overlay-hover)]` with `bg-paper-depth`. Replace `hover:bg-[var(--overlay)]` with `hover:bg-paper-warm`. Replace `text-[var(--foreground)]` with `text-ink`. Replace `focus:ring-[var(--ring)]` with `focus:ring-accent`. Replace `focus:ring-offset-[var(--background)]` with `focus:ring-offset-paper`.

**BackToTop.tsx**: Apply same pattern as ThemeToggle — replace all old var() references and old Tailwind class names.

**LanguageSwitcher.tsx**: Replace `text-foreground` with `text-ink`. Replace `bg-overlay-hover` with `bg-paper-warm`. Replace `text-foreground-muted` with `text-ink-secondary`. Replace `hover:text-foreground` with `hover:text-ink`.
  </action>
  <verify>
    <automated>cd /Users/ekitcho/Desktop/dev-claude-lab/turfu-org-landing && ! grep -rn "foreground\|turfu-accent\|turfu-dark\|turfu-muted\|bg-surface\|bg-overlay\|container-narrow\|bg-gradient-turfu\|var(--surface\|var(--text-\|var(--overlay\|var(--ring)" src/components/Navbar.tsx src/components/Footer.tsx src/components/ThemeToggle.tsx src/components/BackToTop.tsx src/components/LanguageSwitcher.tsx && echo "PASS: no old tokens found"</automated>
  </verify>
  <acceptance_criteria>
    - Navbar.tsx contains zero occurrences of: foreground, surface, turfu-accent, overlay, turfu-dark, turfu-muted
    - Footer.tsx contains zero occurrences of: foreground, surface, turfu-accent, overlay, container-narrow, bg-gradient-turfu
    - Footer.tsx contains `text-accent` (replacing gradient brand text)
    - ThemeToggle.tsx contains `bg-paper-depth` and `text-ink` (replacing old var() references)
    - BackToTop.tsx contains zero occurrences of old token names
    - LanguageSwitcher.tsx uses `text-ink`, `text-ink-secondary`, `bg-paper-warm`
  </acceptance_criteria>
  <done>All 5 layout components use new stone palette tokens. No references to old zinc/gray/gradient tokens remain.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate content, section, admin components and page files</name>
  <files>
    src/components/sections/Hero.tsx,
    src/components/sections/Vision.tsx,
    src/components/sections/Problem.tsx,
    src/components/sections/Principles.tsx,
    src/components/sections/Architecture.tsx,
    src/components/sections/CTA.tsx,
    src/components/sections/Ecosystem.tsx,
    src/components/content/TableOfContents.tsx,
    src/components/content/MarkdownRenderer.tsx,
    src/components/content/MobileSidebarDrawer.tsx,
    src/components/content/ArticleCard.tsx,
    src/components/content/ContentSidebar.tsx,
    src/components/admin/ArticleEditor.tsx,
    src/components/admin/TagInput.tsx,
    src/components/admin/ComboboxInput.tsx,
    src/components/admin/AdminDashboard.tsx,
    src/components/admin/SaveIndicator.tsx,
    src/app/[locale]/admin/login/page.tsx,
    src/app/[locale]/content/page.tsx,
    src/app/[locale]/content/layout.tsx,
    src/app/[locale]/content/loading.tsx,
    src/app/[locale]/content/[slug]/page.tsx,
    src/app/[locale]/content/[slug]/loading.tsx
  </files>
  <read_first>
    - src/components/content/MarkdownRenderer.tsx (heaviest token usage — 20+ replacements)
    - src/components/content/MobileSidebarDrawer.tsx (second heaviest)
    - src/components/admin/ArticleEditor.tsx (admin component with many tokens)
    - src/components/admin/AdminDashboard.tsx
    - src/components/sections/Hero.tsx
    - src/components/sections/CTA.tsx
    - src/components/content/ArticleCard.tsx
    - src/components/content/ContentSidebar.tsx
    - src/components/content/TableOfContents.tsx
    - src/components/admin/TagInput.tsx
    - src/components/admin/ComboboxInput.tsx
    - src/components/admin/SaveIndicator.tsx
    - src/components/sections/Vision.tsx
    - src/components/sections/Problem.tsx
    - src/components/sections/Principles.tsx
    - src/components/sections/Architecture.tsx
    - src/components/sections/Ecosystem.tsx
    - src/app/[locale]/admin/login/page.tsx
    - src/app/[locale]/content/page.tsx
    - src/app/[locale]/content/layout.tsx
    - src/app/[locale]/content/loading.tsx
    - src/app/[locale]/content/[slug]/page.tsx
    - src/app/[locale]/content/[slug]/loading.tsx
    - tailwind.config.ts (confirm available tokens)
  </read_first>
  <action>
Apply the complete token mapping from the interfaces block to ALL 23 files listed. This is a systematic find-and-replace operation. Process each file:

**For every file, apply these replacements in order:**

1. `text-foreground-muted` -> `text-ink-secondary`
2. `text-foreground-secondary` -> `text-ink-secondary`
3. `text-foreground` -> `text-ink` (after the more specific ones above)
4. `bg-surface-elevated` -> `bg-paper-warm`
5. `bg-surface-muted` -> `bg-paper-depth`
6. `bg-surface` -> `bg-paper`
7. `bg-overlay-hover` -> `bg-paper-warm`
8. `bg-overlay` -> `bg-paper-depth`
9. `text-turfu-accent2` -> `text-accent`
10. `hover:text-turfu-accent2` -> `hover:text-accent-hover`
11. `text-turfu-accent` -> `text-accent`
12. `hover:text-turfu-accent` -> `hover:text-accent`
13. `bg-turfu-accent/20` -> `bg-accent-light`
14. `bg-turfu-accent` -> `bg-accent`
15. `ring-turfu-accent` -> `ring-accent`
16. `focus:ring-turfu-accent` -> `focus:ring-accent`
17. `focus:border-turfu-accent` -> `focus:border-accent`
18. `shadow-turfu-accent` -> `shadow-accent`
19. `text-turfu-muted` -> `text-ink-tertiary`
20. `bg-turfu-dark` -> `bg-paper`
21. `bg-turfu-darker` -> `bg-paper`
22. `border-border-muted` -> `border-border`
23. `overlay-hover` -> `paper-warm` (for any remaining references)
24. `container-narrow` -> `max-w-layout mx-auto px-4 sm:px-6 lg:px-8`
25. `btn-primary` -> `inline-flex items-center justify-center px-6 py-3 bg-accent hover:bg-accent-hover text-white font-medium rounded-lg transition-colors`
26. `btn-secondary` -> `inline-flex items-center justify-center px-6 py-3 border border-border hover:border-ink-tertiary text-ink font-medium rounded-lg transition-colors`

**Additional font class additions in MarkdownRenderer.tsx:**
- H1 elements: add `font-display` class
- H2 elements: add `font-display` class
- Code/pre elements: add `font-mono` class if not already present

**Additional font class additions in section components (Hero.tsx, etc.):**
- Any H1 element: add `font-display text-display-1` if appropriate
- Any H2 section title: add `font-display text-display-2` if appropriate

**For inline `var()` references** (found in ThemeToggle, Navbar, Footer — mostly handled in Task 1, but check all files):
- `var(--background)` -> use `bg-paper` Tailwind class instead
- `var(--foreground)` -> use `text-ink` Tailwind class instead
- `var(--surface-*)` -> use `bg-paper-*` Tailwind class instead
- `var(--text-*)` -> use `text-ink-*` Tailwind class instead
- `var(--overlay*)` -> use `bg-paper-depth` / `bg-paper-warm` Tailwind class instead
- `var(--ring)` -> use `ring-accent` Tailwind class instead
- `var(--border-muted)` -> use `border-border` Tailwind class instead
- `rgb(var(--turfu-accent) / N)` -> use accent-light or accent with opacity

After all replacements, verify no file imports or references `Inter` font (should have been handled in 08-01 but check layout imports).
  </action>
  <verify>
    <automated>cd /Users/ekitcho/Desktop/dev-claude-lab/turfu-org-landing && echo "=== Checking for old tokens ===" && HITS=$(grep -rn "text-foreground\|bg-surface\|text-turfu\|bg-turfu\|turfu-accent\|turfu-dark\|turfu-muted\|bg-overlay\|overlay-hover\|container-narrow\|btn-primary\|btn-secondary\|bg-gradient-turfu" src/components/ src/app/[locale]/ --include="*.tsx" | grep -v "node_modules" | wc -l) && echo "Old token hits: $HITS" && if [ "$HITS" -eq 0 ]; then echo "PASS"; else echo "FAIL — old tokens remain"; grep -rn "text-foreground\|bg-surface\|text-turfu\|bg-turfu\|turfu-accent\|turfu-dark\|turfu-muted\|bg-overlay\|overlay-hover\|container-narrow\|btn-primary\|btn-secondary\|bg-gradient-turfu" src/components/ src/app/[locale]/ --include="*.tsx" | head -20; fi</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `text-foreground` in any .tsx file under src/
    - Zero occurrences of `bg-surface` in any .tsx file under src/
    - Zero occurrences of `text-turfu-accent` in any .tsx file under src/
    - Zero occurrences of `bg-turfu` in any .tsx file under src/
    - Zero occurrences of `bg-overlay` in any .tsx file under src/
    - Zero occurrences of `container-narrow` in any .tsx file under src/
    - Zero occurrences of `btn-primary` or `btn-secondary` in any .tsx file under src/
    - Zero occurrences of `bg-gradient-turfu` in any .tsx file under src/
    - MarkdownRenderer.tsx H1 elements contain `font-display`
    - MarkdownRenderer.tsx H2 elements contain `font-display`
    - Hero.tsx H1 element contains `font-display`
    - `npx next build` completes without errors (warnings acceptable)
  </acceptance_criteria>
  <done>All 23 component and page files migrated to new stone palette tokens. Zero references to old zinc/gray/turfu tokens remain in any .tsx file. H1/H2 headings use font-display (Instrument Serif). The build completes successfully.</done>
</task>

<task type="auto">
  <name>Task 3: Final verification — build, dark mode, and codebase audit</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (verify no old tokens leaked back in)
    - tailwind.config.ts (verify clean config)
  </read_first>
  <action>
Run three verification checks:

1. **Codebase-wide audit**: Search the entire `src/` directory for ANY remaining old token references. The following patterns must return zero hits:
   - `--turfu-accent` (old CSS variable)
   - `--surface` (old CSS variable, except inside comments)
   - `--text-primary` (old CSS variable)
   - `--text-secondary` (old CSS variable)
   - `--text-muted` (old CSS variable)
   - `--overlay` (old CSS variable)
   - `turfu-dark` (old Tailwind class)
   - `turfu-darker` (old Tailwind class)
   - `turfu-muted` (old Tailwind class)
   - `'Inter'` (old font reference — but NOT `NextIntlClientProvider` which contains "Int" legitimately)

2. **Build check**: Run `npx next build` and verify it completes. Collect any errors (not warnings).

3. **Dark mode token check**: Verify globals.css has exactly these dark mode overrides and NO extra old variables:
   - `.dark` block contains `--ink: #F5F5F4`
   - `.dark` block contains `--paper: #1C1917`
   - `.dark` block contains `--paper-warm: #292524`
   - `.dark` block contains `--paper-depth: #44403C`
   - `.dark` block contains `--border: #57534E`
   - `.dark` block does NOT contain `--surface`, `--text-`, `--overlay`, `--turfu-accent`, or `--ring`

If any old tokens are found, fix them. If the build fails due to missing classes, add the necessary Tailwind classes or fix the references.
  </action>
  <verify>
    <automated>cd /Users/ekitcho/Desktop/dev-claude-lab/turfu-org-landing && echo "=== Old CSS vars ===" && grep -rn "\-\-turfu-accent\|\-\-surface\|\-\-text-primary\|\-\-text-secondary\|\-\-text-muted\|\-\-overlay\|turfu-dark\|turfu-darker\|turfu-muted" src/ --include="*.css" --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v ".planning" | wc -l && echo "=== Old font ===" && grep -rn "'Inter'" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v ".planning" | wc -l && echo "=== Build ===" && npx next build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep for old CSS variables (`--turfu-accent`, `--surface`, `--text-primary`, `--text-secondary`, `--text-muted`, `--overlay`) returns 0 hits across all src/ files
    - grep for `'Inter'` returns 0 hits across src/ .tsx/.ts files
    - grep for `turfu-dark`, `turfu-darker`, `turfu-muted` returns 0 hits
    - `npx next build` exits with code 0
    - globals.css `.dark` block contains `--ink: #F5F5F4` and `--paper: #1C1917`
    - globals.css `.dark` block does NOT contain any old variable names
  </acceptance_criteria>
  <done>Zero old tokens anywhere in the codebase. Build passes. Dark mode correctly swaps only foundation tokens (ink, paper, border) while keeping layer accents and signal colors identical. The design system migration is complete.</done>
</task>

</tasks>

<verification>
Full phase verification after all plans complete:
1. `grep -rn "turfu-accent\|turfu-dark\|turfu-muted\|bg-surface\|text-foreground\|bg-overlay\|container-narrow\|btn-primary\|btn-secondary\|bg-gradient-turfu" src/ --include="*.tsx" --include="*.ts" --include="*.css" | grep -v node_modules | grep -v ".planning"` returns zero hits
2. `grep "'Inter'" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules` returns zero hits
3. `grep "#1C1917" src/app/globals.css` returns at least 1 match (stone-900 used)
4. `grep "#7C3AED" src/app/globals.css` returns 1 match (layer-0 violet)
5. `grep "Instrument_Serif" src/app/[locale]/layout.tsx` returns 1 match
6. `grep "font-display" src/components/content/MarkdownRenderer.tsx` returns at least 1 match (H1/H2 use serif)
7. `npx next build` exits cleanly
8. Dev server: light mode shows warm stone-50 background, dark mode shows stone-900 background
9. Dev server: H1 renders in Instrument Serif, body in DM Sans
</verification>

<success_criteria>
- All 28 component/page files migrated from old tokens to new stone palette tokens
- Zero references to old zinc/gray/Inter tokens in the entire src/ directory
- H1 and H2 headings use font-display class (Instrument Serif)
- Dark mode toggles cleanly — background flips from stone-50 to stone-900, text from stone-900 to stone-100
- Layer accent colors (violet, teal, orange) available and unchanged between light/dark
- Build passes with zero errors
- Old gradient, btn-primary/secondary, container-narrow utilities fully removed or inlined
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-system-foundation/08-02-SUMMARY.md`
</output>
