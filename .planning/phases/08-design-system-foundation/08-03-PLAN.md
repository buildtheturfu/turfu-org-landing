---
phase: 08-design-system-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Architecture.tsx
  - src/components/sections/Ecosystem.tsx
autonomous: true
gap_closure: true
requirements:
  - DS-04

must_haves:
  truths:
    - "Architecture.tsx uses layer token colors: violet for L0, teal for L1, orange for L2"
    - "Ecosystem.tsx uses layer token colors: violet for L0, teal for L1, orange for L2"
    - "No hardcoded amber/cyan/rose/emerald color classes remain in either component"
    - "No hardcoded rgb() values remain in Architecture.tsx accentColors constant"
  artifacts:
    - path: "src/components/sections/Architecture.tsx"
      provides: "Architecture section with layer token colors"
      contains: "layer-0"
    - path: "src/components/sections/Ecosystem.tsx"
      provides: "Ecosystem section with layer token colors"
      contains: "layer-0"
  key_links:
    - from: "src/components/sections/Architecture.tsx"
      to: "tailwind.config.ts"
      via: "layer.0/layer.1/layer.2 Tailwind utilities"
      pattern: "(text|bg)-layer-[012]"
    - from: "src/components/sections/Ecosystem.tsx"
      to: "tailwind.config.ts"
      via: "layer.0/layer.1/layer.2 Tailwind utilities"
      pattern: "(text|bg)-layer-[012]"
---

<objective>
Close the DS-04 verification gap: migrate Architecture.tsx and Ecosystem.tsx from hardcoded color values to the layer design tokens (bg-layer-0, text-layer-0, etc.).

Purpose: The layer tokens (violet L0 #7C3AED, teal L1 #0D9488, orange L2 #EA580C) are defined in globals.css and tailwind.config.ts but zero components consume them. The two components that visually represent the three-layer architecture use wrong, hardcoded colors.
Output: Both components use layer token Tailwind utilities exclusively for layer-coded colors.
</objective>

<execution_context>
@/Users/ekitcho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ekitcho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-design-system-foundation/08-VERIFICATION.md

<interfaces>
<!-- Layer token system defined in globals.css and tailwind.config.ts -->

From src/app/globals.css:
```css
--layer-0: #7C3AED;       /* violet */
--layer-0-light: #EDE9FE;
--layer-1: #0D9488;        /* teal */
--layer-1-light: #CCFBF1;
--layer-2: #EA580C;        /* orange */
--layer-2-light: #FFF7ED;
```

From tailwind.config.ts:
```typescript
layer: {
  0: { DEFAULT: 'var(--layer-0)', light: 'var(--layer-0-light)' },
  1: { DEFAULT: 'var(--layer-1)', light: 'var(--layer-1-light)' },
  2: { DEFAULT: 'var(--layer-2)', light: 'var(--layer-2-light)' },
}
```

Available Tailwind utilities:
- `text-layer-0`, `text-layer-1`, `text-layer-2` (text color)
- `bg-layer-0`, `bg-layer-1`, `bg-layer-2` (background)
- `bg-layer-0-light`, `bg-layer-1-light`, `bg-layer-2-light` (light backgrounds)
- `border-layer-0`, `border-layer-1`, `border-layer-2` (borders)
- Opacity modifiers work: `bg-layer-0/20`, `text-layer-1/50`, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Architecture.tsx to layer tokens</name>
  <files>src/components/sections/Architecture.tsx</files>
  <read_first>
    - src/components/sections/Architecture.tsx
    - src/app/globals.css (lines 14-19 for layer token values)
    - tailwind.config.ts (lines 24-27 for layer Tailwind mappings)
  </read_first>
  <action>
Replace all hardcoded color references in Architecture.tsx with layer design tokens. Specific replacements:

**1. Layer config object (lines 43-92) — `accent` and `accentBg` fields:**
- layer0: `accent: 'text-amber-400'` -> `accent: 'text-layer-0'`
- layer0: `accentBg: 'bg-amber-400'` -> `accentBg: 'bg-layer-0'`
- layer1: `accent: 'text-cyan-400'` -> `accent: 'text-layer-1'`
- layer1: `accentBg: 'bg-cyan-400'` -> `accentBg: 'bg-layer-1'`
- layer2: `accent: 'text-rose-400'` -> `accent: 'text-layer-2'`
- layer2: `accentBg: 'bg-rose-400'` -> `accentBg: 'bg-layer-2'`

**2. Layer config object — `gradient` fields:**
- layer0: `gradient: 'from-[#0D1B2A]/90 to-[#0D1B2A]/70'` -> `gradient: 'from-layer-0/10 to-layer-0/5'`
- layer1: `gradient: 'from-[#1B3A4B]/90 to-[#1B3A4B]/70'` -> `gradient: 'from-layer-1/10 to-layer-1/5'`
- layer2: `gradient: 'from-[#2D5A6B]/90 to-[#2D5A6B]/70'` -> `gradient: 'from-layer-2/10 to-layer-2/5'`

**3. `accentColors` JS constant (lines 94-98) — replace hardcoded rgb() values:**
Replace entire constant with CSS variable references:
```typescript
const accentColors: Record<LayerKey, string> = {
  layer0: 'var(--layer-0)',
  layer1: 'var(--layer-1)',
  layer2: 'var(--layer-2)',
};
```

**4. Section background (line 123) — hardcoded inline style:**
Replace `style={{ background: 'linear-gradient(135deg, #0D1B2A 0%, #1B3A4B 50%, #2D5A6B 100%)' }}` with a class-based approach using the stone dark palette:
`className="... bg-paper-depth"` (remove the inline gradient style entirely — the section already has `dark` class forcing dark mode)

**5. Background glow effects (lines 126-128):**
- `bg-amber-500/[0.08]` -> `bg-layer-0/[0.08]`
- `bg-cyan-500/[0.08]` -> `bg-layer-1/[0.08]`
- `bg-rose-500/[0.05]` -> `bg-layer-2/[0.05]`

**6. Badge, title highlight, and equation section (lines 148-196):**
- Line 148: `bg-amber-400/15 border border-amber-400/30 ... text-amber-400` -> `bg-layer-0/15 border border-layer-0/30 ... text-layer-0`
- Line 152: `from-white via-amber-400 to-cyan-400` -> `from-white via-layer-0 to-layer-1`
- Line 159: `text-amber-400` -> `text-layer-0`
- Equation items (line 172-174): Change `color` field from `'amber'`/`'cyan'`/`'rose'` to `'layer0'`/`'layer1'`/`'layer2'` and update the inline style rgba values to use `var(--layer-0)`, `var(--layer-1)`, `var(--layer-2)` respectively. Replace the ternary conditionals for text color classes:
  - `'text-amber-400'` -> `'text-layer-0'`
  - `'text-cyan-400'` -> `'text-layer-1'`
  - `'text-rose-400'` -> `'text-layer-2'`
- Inline rgba background styles in equation items: use CSS variable approach — set background/borderColor using the accentColors[layerKey] pattern or define helper. Example: `style={{ background: \`color-mix(in srgb, var(--layer-0) 15%, transparent)\` }}`
- Line 191: `text-cyan-400` -> `text-layer-1`
- Line 192: `from-amber-400/20 to-cyan-400/20 border-2 border-amber-400/50` -> `from-layer-0/20 to-layer-1/20 border-2 border-layer-0/50`
- Line 194: `text-amber-400` -> `text-layer-0`

**7. Flow section and key concepts (lines 316-406):**
- Line 323: `text-amber-400` -> `text-layer-0`
- Line 330: `bg-amber-400/20 border border-amber-400/40 text-amber-400` -> `bg-layer-0/20 border border-layer-0/40 text-layer-0`
- Line 388: `text-amber-400` -> `text-layer-0`
- Line 389: `text-amber-400` -> `text-layer-0`
- Line 405: `text-amber-400` -> `text-layer-0`

**Important:** For all inline styles that use `accentColors[layerKey]` with string manipulation (`.replace('rgb', 'rgba').replace(')', ' / 0.2)')`) — since accentColors now uses `var(--layer-*)` format, replace these patterns with `color-mix(in srgb, var(--layer-*) 20%, transparent)` or use Tailwind opacity utilities where possible. Prefer Tailwind classes over inline styles wherever feasible.
  </action>
  <verify>
    <automated>cd /Users/ekitcho/Desktop/dev-claude-lab/turfu-org-landing && grep -cE "amber-400|cyan-400|rose-400|rgb\(251|rgb\(34|#0D1B2A|#1B3A4B|#2D5A6B" src/components/sections/Architecture.tsx | grep -q "^0$" && echo "PASS: no hardcoded colors" || echo "FAIL: hardcoded colors remain"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "text-amber-400" in Architecture.tsx returns 0 matches
    - grep for "text-cyan-400" in Architecture.tsx returns 0 matches
    - grep for "text-rose-400" in Architecture.tsx returns 0 matches
    - grep for "rgb(251" in Architecture.tsx returns 0 matches
    - grep for "rgb(34" in Architecture.tsx returns 0 matches
    - grep for "#0D1B2A" in Architecture.tsx returns 0 matches
    - grep for "text-layer-0" in Architecture.tsx returns at least 1 match
    - grep for "text-layer-1" in Architecture.tsx returns at least 1 match
    - grep for "text-layer-2" in Architecture.tsx returns at least 1 match
    - grep for "var(--layer-0)" in Architecture.tsx returns at least 1 match
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Architecture.tsx uses layer-0/layer-1/layer-2 tokens exclusively for all layer-coded colors. Zero hardcoded amber/cyan/rose classes, zero hardcoded rgb() values, zero hardcoded hex gradients from the old dark palette.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Ecosystem.tsx to layer tokens</name>
  <files>src/components/sections/Ecosystem.tsx</files>
  <read_first>
    - src/components/sections/Ecosystem.tsx
    - src/app/globals.css (lines 14-19 for layer token values)
    - tailwind.config.ts (lines 24-27 for layer Tailwind mappings)
  </read_first>
  <action>
Replace all hardcoded gradient color classes in Ecosystem.tsx with layer design tokens. Specific replacements:

**1. Layer array (lines 10-14) — `color` field:**
- `{ key: 'layer0', icon: Shield, color: 'from-violet-500 to-purple-600' }` -> `{ key: 'layer0', icon: Shield, color: 'bg-layer-0' }`
- `{ key: 'layer1', icon: Rocket, color: 'from-cyan-500 to-blue-600' }` -> `{ key: 'layer1', icon: Rocket, color: 'bg-layer-1' }`
- `{ key: 'layer2', icon: Boxes, color: 'from-emerald-500 to-teal-600' }` -> `{ key: 'layer2', icon: Boxes, color: 'bg-layer-2' }`

**2. Hover overlay gradient (line 39):**
Change from `bg-gradient-to-br ${layer.color}` to just `${layer.color}` since color is now a single bg-* class, not a gradient. The opacity-0 group-hover:opacity-10 pattern stays the same.

**3. Icon container (line 44):**
Change from `bg-gradient-to-br ${layer.color}` to just `${layer.color}` since it is now a single bg-* class.

Both changes are straightforward: the gradient classes (`from-X to-Y`) become a single `bg-layer-N` class. The visual result is a solid layer-colored background instead of a gradient, which is more aligned with the design system's token approach.
  </action>
  <verify>
    <automated>cd /Users/ekitcho/Desktop/dev-claude-lab/turfu-org-landing && grep -cE "violet-500|purple-600|cyan-500|blue-600|emerald-500|teal-600" src/components/sections/Ecosystem.tsx | grep -q "^0$" && echo "PASS: no hardcoded colors" || echo "FAIL: hardcoded colors remain"</automated>
  </verify>
  <acceptance_criteria>
    - grep for "violet-500" in Ecosystem.tsx returns 0 matches
    - grep for "cyan-500" in Ecosystem.tsx returns 0 matches
    - grep for "emerald-500" in Ecosystem.tsx returns 0 matches
    - grep for "bg-layer-0" in Ecosystem.tsx returns at least 1 match
    - grep for "bg-layer-1" in Ecosystem.tsx returns at least 1 match
    - grep for "bg-layer-2" in Ecosystem.tsx returns at least 1 match
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Ecosystem.tsx uses bg-layer-0, bg-layer-1, bg-layer-2 tokens for all layer-coded colors. Zero hardcoded violet/cyan/emerald/purple/blue/teal gradient classes.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -rE "amber-400|cyan-400|rose-400|violet-500|cyan-500|emerald-500|purple-600|blue-600|teal-600" src/components/sections/Architecture.tsx src/components/sections/Ecosystem.tsx` returns zero matches
2. `grep -rE "(text|bg|border)-layer-[012]" src/components/sections/Architecture.tsx src/components/sections/Ecosystem.tsx` returns multiple matches per file
3. `grep -c "rgb(" src/components/sections/Architecture.tsx` returns 0 (no hardcoded rgb values)
4. `npm run build` succeeds with no errors
</verification>

<success_criteria>
- DS-04 gap closed: layer tokens are consumed by the two components that visually represent the three-layer architecture
- Architecture.tsx uses text-layer-0/text-layer-1/text-layer-2 and bg-layer-0/bg-layer-1/bg-layer-2 for all layer-coded visual elements
- Ecosystem.tsx uses bg-layer-0/bg-layer-1/bg-layer-2 for icon backgrounds and hover overlays
- Zero hardcoded non-token color references remain in either file
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/08-design-system-foundation/08-03-SUMMARY.md`
</output>
