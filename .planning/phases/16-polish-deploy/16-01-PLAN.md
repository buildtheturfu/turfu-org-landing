---
phase: 16-polish-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/Navbar.tsx
  - src/components/Footer.tsx
  - src/components/sections/Hero.tsx
  - src/components/sections/Ecosystem.tsx
  - src/components/sections/CTA.tsx
  - src/components/sections/LatestPublications.tsx
  - src/app/[locale]/page.tsx
  - src/app/[locale]/vision/page.tsx
  - src/app/[locale]/publications/page.tsx
  - src/app/[locale]/publications/[slug]/page.tsx
  - src/app/[locale]/ecosystem/page.tsx
  - src/app/[locale]/ecosystem/[slug]/page.tsx
  - src/app/[locale]/join/page.tsx
  - src/app/[locale]/research/page.tsx
  - src/app/[locale]/admin/page.tsx
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02

must_haves:
  truths:
    - "All pages render without horizontal overflow at 375px mobile width"
    - "All pages render with correct grid layouts at 768px tablet"
    - "All pages render at full width at 1440px desktop"
    - "Dark mode shows no invisible text, no broken colors, no mismatched backgrounds on any page"
    - "Dark mode toggle transitions cleanly with no flash on every page"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Dark mode CSS variable fixes if any found"
    - path: "src/components/sections/Hero.tsx"
      provides: "Responsive hero section"
    - path: "src/components/sections/Ecosystem.tsx"
      provides: "Responsive ecosystem preview"
  key_links:
    - from: "globals.css"
      to: "all page components"
      via: "CSS custom properties (--ink, --paper, --border, --accent-*)"
      pattern: "var\\(--"
---

<objective>
Audit and fix responsive layout and dark mode consistency across all site pages.

Purpose: Ensure production-quality visual experience at all breakpoints (375px, 768px, 1440px) and in both light/dark themes. This is the visual polish pass before deployment.

Output: All pages pass responsive and dark mode audit with zero visual regressions.
</objective>

<execution_context>
@/Users/ekitcho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ekitcho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/globals.css
@tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Responsive audit and fixes across all pages</name>
  <files>
    src/app/globals.css,
    src/components/Navbar.tsx,
    src/components/Footer.tsx,
    src/components/sections/Hero.tsx,
    src/components/sections/Ecosystem.tsx,
    src/components/sections/CTA.tsx,
    src/components/sections/LatestPublications.tsx,
    src/app/[locale]/vision/page.tsx,
    src/app/[locale]/publications/page.tsx,
    src/app/[locale]/publications/[slug]/page.tsx,
    src/app/[locale]/ecosystem/page.tsx,
    src/app/[locale]/ecosystem/[slug]/page.tsx,
    src/app/[locale]/join/page.tsx,
    src/app/[locale]/research/page.tsx,
    src/app/[locale]/admin/page.tsx
  </files>
  <read_first>
    src/app/globals.css,
    tailwind.config.ts,
    src/components/Navbar.tsx,
    src/components/Footer.tsx,
    src/components/sections/Hero.tsx,
    src/components/sections/Ecosystem.tsx,
    src/components/sections/CTA.tsx,
    src/components/sections/LatestPublications.tsx,
    src/app/[locale]/page.tsx,
    src/app/[locale]/vision/page.tsx,
    src/app/[locale]/publications/page.tsx,
    src/app/[locale]/publications/[slug]/page.tsx,
    src/app/[locale]/ecosystem/page.tsx,
    src/app/[locale]/ecosystem/[slug]/page.tsx,
    src/app/[locale]/join/page.tsx,
    src/app/[locale]/research/page.tsx,
    src/app/[locale]/admin/page.tsx
  </read_first>
  <action>
    Systematically audit each page file for responsive issues at 375px, 768px, and 1440px breakpoints. Fix issues found:

    1. **Overflow check:** Search for fixed widths (w-[NNpx], min-w-[NNpx]) that could overflow on mobile. Replace with responsive equivalents (max-w-full, w-full on mobile).

    2. **Grid/flex audit:** Verify all grid layouts collapse properly on mobile:
       - grid-cols-2/3/4 must have sm: or md: prefix (not bare)
       - flex-row layouts must wrap or become flex-col on mobile
       - gap values should be reasonable at 375px

    3. **Typography audit:** Verify text sizes are readable at 375px:
       - H1 should scale down (text-3xl sm:text-4xl lg:text-5xl pattern)
       - Body text must not be smaller than 14px on mobile
       - No text-truncation that hides critical content

    4. **Padding/margin audit:** Verify horizontal padding is at least px-4 on mobile for all page containers. No content should touch screen edges.

    5. **Image audit:** All next/image usages must have responsive sizing (sizes prop with breakpoint values or fill with proper container).

    6. **Navbar:** Verify hamburger menu works at 375px, all nav links accessible.

    7. **Footer:** Verify footer grid collapses to single column on mobile.

    8. **Admin page:** The admin dashboard table/list must be scrollable or stack on mobile.

    Apply Tailwind responsive utilities (sm:, md:, lg:) to fix any issues found. Use mobile-first approach: base styles for 375px, then layer up.
  </action>
  <verify>
    <automated>cd /Users/ekitcho/Desktop/dev-claude-lab/turfu-org-landing && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "w-\[.*px\]" src/components/sections/ src/app/\[locale\]/ returns zero results without responsive prefix OR all fixed widths are intentional (max-w containers)
    - All grid-cols-N usages in page/section components have mobile breakpoint variants (grep confirms sm: or md: prefix pattern)
    - Build succeeds with no errors
  </acceptance_criteria>
  <done>All pages have responsive Tailwind classes for 375px/768px/1440px. No horizontal overflow at mobile. Grids collapse properly. Text is readable at all sizes.</done>
</task>

<task type="auto">
  <name>Task 2: Dark mode audit and fixes across all pages</name>
  <files>
    src/app/globals.css,
    src/components/sections/Hero.tsx,
    src/components/sections/Ecosystem.tsx,
    src/components/sections/CTA.tsx,
    src/components/sections/LatestPublications.tsx,
    src/app/[locale]/vision/page.tsx,
    src/app/[locale]/publications/page.tsx,
    src/app/[locale]/publications/[slug]/page.tsx,
    src/app/[locale]/ecosystem/page.tsx,
    src/app/[locale]/ecosystem/[slug]/page.tsx,
    src/app/[locale]/join/page.tsx,
    src/app/[locale]/research/page.tsx
  </files>
  <read_first>
    src/app/globals.css (focus on .dark section and CSS custom properties),
    src/components/sections/Hero.tsx,
    src/components/sections/Ecosystem.tsx,
    src/components/sections/CTA.tsx,
    src/components/sections/LatestPublications.tsx,
    src/app/[locale]/vision/page.tsx,
    src/app/[locale]/ecosystem/page.tsx,
    src/app/[locale]/ecosystem/[slug]/page.tsx,
    src/app/[locale]/join/page.tsx,
    src/app/[locale]/research/page.tsx
  </read_first>
  <action>
    Systematically audit each page and component for dark mode issues. The project uses CSS custom properties (--ink, --paper, --border, --accent-*) via the design system from Phase 8. Fix issues found:

    1. **Hardcoded colors check:** Search for any hardcoded color values (text-black, text-white, bg-white, bg-black, text-gray-*, bg-gray-*, text-stone-*, bg-stone-*) that are NOT wrapped in dark: variants. These must use semantic tokens instead:
       - text-black/text-white -> text-ink
       - bg-white/bg-black -> bg-paper
       - border-gray-* -> border-border
       - Any bg-stone-* that's not a semantic variable -> replace with bg-paper, bg-paper-warm, or bg-paper-depth

    2. **Inline style check:** Search for inline style={{ color: or backgroundColor: that uses hardcoded hex values instead of CSS variables. Replace with var(--ink), var(--paper), etc.

    3. **Shadow check:** Verify box shadows (shadow-sm, shadow-md, shadow-lg) look appropriate in dark mode. If too harsh, add dark:shadow-none or use border instead.

    4. **Border check:** Verify borders are visible in both modes. border-gray-200 disappears in dark mode -- must use border-border or dark: variant.

    5. **Image/icon contrast:** Verify icons (lucide-react) inherit text color properly. Check featured images have proper dark mode backgrounds/borders.

    6. **Focus/hover states:** Verify focus rings and hover backgrounds work in both modes.

    7. **Specific pages to watch (added in phases 13-15):**
       - Hero: bg-paper background, text-ink heading
       - LatestPublications: bg-paper-warm background, card borders
       - Ecosystem preview: layer accent colors (violet, teal, orange) should stay same in dark
       - CTA: bg-paper-warm background
       - Vision page: ProseLayout text readability
       - Ecosystem product pages: card backgrounds
       - Join page: CTA button colors
  </action>
  <verify>
    <automated>cd /Users/ekitcho/Desktop/dev-claude-lab/turfu-org-landing && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "text-black\|text-white\|bg-white\|bg-black" src/components/sections/ src/app/\[locale\]/ returns zero hits (excluding dark: prefixed variants and Tailwind config)
    - grep -rn "text-gray-\|bg-gray-\|border-gray-" src/components/sections/ src/app/\[locale\]/ returns zero hits without dark: variant or returns only intentional usages
    - All color usages in page components use semantic tokens (text-ink, bg-paper, border-border, bg-paper-warm, bg-paper-depth)
    - Build succeeds
  </acceptance_criteria>
  <done>All pages use semantic color tokens. No hardcoded colors that break in dark mode. Dark mode renders correctly on every page with proper contrast and no invisible text.</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. No hardcoded color classes in page/section components (semantic tokens only)
3. All grid layouts have mobile-responsive breakpoint variants
</verification>

<success_criteria>
- All pages build successfully
- Responsive: No horizontal overflow at 375px, proper grid collapse, readable text
- Dark mode: Semantic tokens everywhere, no hardcoded colors, proper contrast
- DEPLOY-01 and DEPLOY-02 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/16-polish-deploy/16-01-SUMMARY.md`
</output>
