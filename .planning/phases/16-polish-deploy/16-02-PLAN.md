---
phase: 16-polish-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/messages/en.json
  - src/messages/tr.json
  - src/messages/fr.json
  - src/app/[locale]/content/page.tsx
  - src/app/[locale]/content/[slug]/page.tsx
  - src/app/[locale]/content/layout.tsx
  - src/components/content/ArticleCard.tsx
  - src/components/content/ContentSidebar.tsx
  - src/components/content/MarkdownRenderer.tsx
  - src/components/content/MobileSidebarDrawer.tsx
  - src/components/content/TableOfContents.tsx
  - src/lib/articles.ts
  - src/lib/__tests__/articles.test.ts
  - src/lib/schemas/article.ts
  - src/app/api/admin/articles/route.ts
  - src/app/api/admin/articles/[id]/route.ts
  - src/app/api/admin/categories/route.ts
  - src/app/api/admin/tags/route.ts
  - src/app/sitemap.ts
  - src/app/robots.ts
  - next.config.js
autonomous: false
requirements:
  - DEPLOY-03
  - DEPLOY-04
  - DEPLOY-05

must_haves:
  truths:
    - "EN translations are real English content (not FR placeholders) for all namespaces"
    - "TR translations have all keys present (FR fallback acceptable for long-form content)"
    - "No missing translation keys cause runtime errors in any locale"
    - "Legacy article system files are removed with no remaining imports"
    - "Lighthouse mobile score exceeds 90 on home page"
    - "DNS deployment steps are documented for the user"
  artifacts:
    - path: "src/messages/en.json"
      provides: "Complete English translations"
      min_lines: 180
    - path: "src/messages/tr.json"
      provides: "Complete Turkish translations"
      min_lines: 180
    - path: "DNS-DEPLOY.md"
      provides: "DNS deployment instructions for user"
  key_links:
    - from: "src/messages/*.json"
      to: "all page components"
      via: "useTranslations() hook"
      pattern: "useTranslations\\("
    - from: "src/app/sitemap.ts"
      to: "all public pages"
      via: "URL list generation"
      pattern: "sitemap"
---

<objective>
Complete i18n translations, remove legacy article system, optimize for Lighthouse, and document DNS deployment.

Purpose: Ensure the site is linguistically complete (FR/EN/TR), free of dead code, performant (Lighthouse > 90), and ready for the user to point turfu.org DNS when satisfied.

Output: Clean codebase with complete translations, optimized performance, and DNS deployment guide.
</objective>

<execution_context>
@/Users/ekitcho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ekitcho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/messages/fr.json
@src/messages/en.json
@src/messages/tr.json
@src/lib/articles.ts
@src/app/sitemap.ts
@next.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: i18n audit and translation completion</name>
  <files>
    src/messages/fr.json,
    src/messages/en.json,
    src/messages/tr.json
  </files>
  <read_first>
    src/messages/fr.json,
    src/messages/en.json,
    src/messages/tr.json
  </read_first>
  <action>
    Audit and complete translations for all three locales.

    1. **EN audit:** Read every value in en.json and compare to fr.json. Any value that is still in French (not translated to English) must be translated. Key namespaces to verify:
       - meta (site title, description)
       - nav (navigation labels)
       - hero (headline, subtitle, CTA labels)
       - latestPublications (section title, see all link)
       - ecosystem (section content)
       - cta (call to action content)
       - footer (all footer text)
       - publications (feed page labels, filters, pagination)
       - article (article page labels)
       - content (content section labels)
       - visionPage (full vision text -- must be proper English, not French)
       - researchPage (research page labels)
       - ecosystemPage (ecosystem page content, product descriptions)
       - joinPage (join page CTAs and descriptions)

    2. **TR audit:** Read every value in tr.json. For UI strings (nav, meta, footer, button labels, section titles), provide proper Turkish translations. For long-form content (visionPage body paragraphs, ecosystem descriptions), FR text as placeholder is acceptable per user decision, but all keys MUST exist. Verify no keys are missing.

    3. **FR audit:** Verify fr.json is complete -- it is the reference locale. Check for any empty string values or placeholder markers like "TODO" or "...".

    4. **Cross-locale consistency:** Verify all three files have identical key structures (same nesting, same keys). Use the FR file as the canonical reference.

    Translation quality notes:
    - EN: Professional quality English. "TURFu" stays as-is (brand name). French terms like "Couche" become "Layer". Keep domain-specific terms.
    - TR: For short UI strings, translate properly. For long-form content, copy FR text as-is if unsure -- user said this is acceptable.
  </action>
  <verify>
    <automated>cd /Users/ekitcho/Desktop/dev-claude-lab/turfu-org-landing && python3 -c "
import json
with open('src/messages/fr.json') as f: fr = json.load(f)
with open('src/messages/en.json') as f: en = json.load(f)
with open('src/messages/tr.json') as f: tr = json.load(f)
def keys(d,p=''):
    r=set()
    for k,v in d.items():
        if isinstance(v,dict): r|=keys(v,f'{p}{k}.')
        else: r.add(f'{p}{k}')
    return r
fk,ek,tk=keys(fr),keys(en),keys(tr)
missing_en=fk-ek
missing_tr=fk-tk
assert not missing_en, f'EN missing: {missing_en}'
assert not missing_tr, f'TR missing: {missing_tr}'
print('All locales have identical key sets')
"</automated>
  </verify>
  <acceptance_criteria>
    - python3 key comparison script passes (all three locales have identical key structures)
    - grep -c '""' src/messages/en.json returns 0 (no empty string values)
    - grep -c '""' src/messages/tr.json returns 0 (no empty string values)
    - EN file contains no French words in nav, hero, footer, cta namespaces (spot-check: grep "Accueil\|Rejoindre\|Construire" src/messages/en.json returns 0)
  </acceptance_criteria>
  <done>All three locale files have identical key structures. EN has proper English translations. TR has proper Turkish UI strings. No empty values or missing keys.</done>
</task>

<task type="auto">
  <name>Task 2: Legacy cleanup, Lighthouse optimization, and DNS documentation</name>
  <files>
    src/app/[locale]/content/page.tsx,
    src/app/[locale]/content/[slug]/page.tsx,
    src/app/[locale]/content/layout.tsx,
    src/app/[locale]/content/loading.tsx,
    src/components/content/ArticleCard.tsx,
    src/components/content/ContentSidebar.tsx,
    src/components/content/MarkdownRenderer.tsx,
    src/components/content/MobileSidebarDrawer.tsx,
    src/components/content/TableOfContents.tsx,
    src/lib/articles.ts,
    src/lib/__tests__/articles.test.ts,
    src/lib/schemas/article.ts,
    src/app/api/admin/articles/route.ts,
    src/app/api/admin/articles/[id]/route.ts,
    src/app/api/admin/categories/route.ts,
    src/app/api/admin/tags/route.ts,
    src/app/sitemap.ts,
    src/app/robots.ts,
    next.config.js,
    DNS-DEPLOY.md
  </files>
  <read_first>
    src/app/sitemap.ts,
    src/app/robots.ts,
    next.config.js,
    src/lib/articles.ts (first 20 lines to confirm it's legacy),
    src/app/[locale]/content/page.tsx (first 10 lines to confirm it imports from articles),
    src/app/api/admin/articles/route.ts (first 10 lines to confirm it's legacy article CRUD),
    src/app/api/admin/tags/route.ts (first 10 lines),
    src/app/api/admin/categories/route.ts (first 10 lines)
  </read_first>
  <action>
    Three sub-tasks: legacy cleanup, Lighthouse prep, DNS docs.

    **A. Legacy article system removal:**

    Delete the following files/directories that belong to the old article system (replaced by publications in phases 10-15):

    Files to DELETE:
    - src/app/[locale]/content/ (entire directory -- page.tsx, [slug]/page.tsx, layout.tsx, loading.tsx)
    - src/components/content/ArticleCard.tsx
    - src/components/content/ContentSidebar.tsx
    - src/components/content/MarkdownRenderer.tsx
    - src/components/content/MobileSidebarDrawer.tsx
    - src/components/content/TableOfContents.tsx
    - src/lib/articles.ts
    - src/lib/__tests__/articles.test.ts
    - src/lib/schemas/article.ts
    - src/app/api/admin/articles/ (entire directory -- route.ts and [id]/route.ts)
    - src/app/api/admin/categories/route.ts
    - src/app/api/admin/tags/route.ts

    DO NOT DELETE these (they are used by the new publication system):
    - src/components/content/ComboboxInput.tsx (used by PublicationEditor)
    - src/components/content/TagInput.tsx (used by PublicationEditor)

    Wait -- check imports first. Before deleting, grep the entire src/ for any imports of the files being deleted. If anything still imports from them (other than the files being deleted themselves), DO NOT delete and instead fix the import.

    After deletion, verify no broken imports remain: run `npx next build`.

    **B. Lighthouse performance optimization:**

    1. Check next.config.js for image optimization settings. Ensure:
       - images.formats includes 'image/webp' (or 'image/avif')
       - Any unused rewrites/redirects are removed

    2. Check for lazy loading opportunities:
       - framer-motion: If imported in page-level components, consider dynamic import with `next/dynamic` and `ssr: false` for below-the-fold sections
       - Large components below the fold: wrap in dynamic() import

    3. Font optimization: Verify fonts load via next/font (not manual link tags) for automatic font optimization.

    4. Update sitemap.ts: Remove old /content routes, ensure all current pages are listed (/vision, /publications, /ecosystem, /join, /research). Verify it generates correct URLs for all locales (fr, en, tr).

    5. Update robots.ts: Ensure it allows crawling of all public pages and disallows /admin.

    **C. DNS deployment documentation:**

    Create DNS-DEPLOY.md at project root with these instructions for the user:
    - Title: "Deploying turfu.org"
    - Step 1: Verify the Vercel preview deployment works (provide the `vercel` CLI command to check)
    - Step 2: In Vercel dashboard > Settings > Domains, add `turfu.org`
    - Step 3: At DNS provider, create:
      - A record: turfu.org -> 76.76.21.21
      - CNAME record: www.turfu.org -> cname.vercel-dns.com
    - Step 4: Wait for DNS propagation (up to 48h)
    - Step 5: Verify SSL certificate auto-provisioned by Vercel
    - Note: "Only proceed when FR, EN, and TR translations are validated"
  </action>
  <verify>
    <automated>cd /Users/ekitcho/Desktop/dev-claude-lab/turfu-org-landing && npx next build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - ls src/lib/articles.ts returns "No such file" (legacy file deleted)
    - ls src/app/[locale]/content/ returns "No such file" (legacy routes deleted)
    - ls src/app/api/admin/articles/ returns "No such file" (legacy API deleted)
    - npx next build succeeds with zero errors
    - test -f DNS-DEPLOY.md (DNS documentation exists)
    - grep "turfu.org" DNS-DEPLOY.md returns matches (DNS instructions present)
    - grep "/admin" src/app/robots.ts shows disallow rule
  </acceptance_criteria>
  <done>Legacy article files deleted. Build passes with no broken imports. Sitemap updated with current routes. robots.ts correct. DNS-DEPLOY.md created with step-by-step instructions. Lighthouse optimizations applied (lazy loading, image config, font config).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final production readiness verification</name>
  <files>DNS-DEPLOY.md</files>
  <action>
    Human checkpoint: verify the complete polish pass visually.
    - Run dev server and test responsive layouts at 375px, 768px, 1440px
    - Toggle dark mode on every page
    - Switch locales to /en and /tr
    - Review DNS-DEPLOY.md
  </action>
  <what-built>
    Complete polish pass: responsive fixes, dark mode fixes, i18n completion (FR/EN/TR), legacy code removal, Lighthouse optimization, and DNS documentation.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000/fr
    2. Test responsive: resize browser to 375px width, navigate all pages (home, /vision, /publications, /ecosystem, /join, /research)
    3. Test dark mode: toggle theme on each page, verify no invisible text or broken colors
    4. Test locales: switch to /en and /tr, verify translations render (no missing key errors in console)
    5. Run `npx next build` and check for warnings
    6. Review DNS-DEPLOY.md for accuracy
  </how-to-verify>
  <verify>
    <automated>npx next build 2>&1 | tail -3</automated>
  </verify>
  <done>User has verified responsive, dark mode, i18n, and DNS documentation. Phase 16 and v3.0 milestone complete.</done>
  <resume-signal>Type "approved" to complete Phase 16 and v3.0, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx next build` completes successfully (no errors, no broken imports)
2. All three locale files have identical key structures
3. No legacy article files remain in the codebase
4. DNS-DEPLOY.md exists with deployment instructions
5. User visually confirms responsive, dark mode, and i18n quality
</verification>

<success_criteria>
- i18n: All three locales structurally complete, EN properly translated, TR has all keys
- Legacy: Zero references to old article system, clean build
- Performance: Lighthouse optimizations applied (lazy loading, image config)
- DNS: Documentation created for user to execute manually
- DEPLOY-03, DEPLOY-04, DEPLOY-05 requirements satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/16-polish-deploy/16-02-SUMMARY.md`
</output>
