---
phase: 14-static-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/products.ts
  - src/app/[locale]/ecosystem/page.tsx
  - src/app/[locale]/ecosystem/[slug]/page.tsx
  - src/app/[locale]/join/page.tsx
  - src/messages/fr.json
  - src/messages/en.json
  - src/messages/tr.json
autonomous: true
requirements: [PAGES-03, PAGES-04, PAGES-05]

must_haves:
  truths:
    - "/ecosystem displays the Layer 0/1/2 schema with product cards"
    - "Clicking a product card navigates to /ecosystem/[slug] with product details"
    - "Product cards have layer-colored accent stripes (violet L0, teal L1, orange L2)"
    - "/join presents 3 contribution CTAs with working external links"
    - "/ecosystem/[slug] shows problem/solution/status/stack/link for each product"
  artifacts:
    - path: "src/data/products.ts"
      provides: "Static typed product data for 7 products across 3 layers"
      exports: ["Product", "products", "getProductBySlug", "getProductsByLayer"]
    - path: "src/app/[locale]/ecosystem/page.tsx"
      provides: "Ecosystem overview with layer cards and product grid"
      contains: "setRequestLocale"
    - path: "src/app/[locale]/ecosystem/[slug]/page.tsx"
      provides: "Product detail page with generateStaticParams"
      contains: "generateStaticParams"
    - path: "src/app/[locale]/join/page.tsx"
      provides: "Join page with contribution CTAs"
      contains: "setRequestLocale"
  key_links:
    - from: "src/app/[locale]/ecosystem/page.tsx"
      to: "src/data/products.ts"
      via: "import { products, getProductsByLayer }"
      pattern: "getProductsByLayer"
    - from: "src/app/[locale]/ecosystem/[slug]/page.tsx"
      to: "src/data/products.ts"
      via: "import { getProductBySlug, products }"
      pattern: "getProductBySlug"
    - from: "src/app/[locale]/ecosystem/page.tsx"
      to: "src/app/[locale]/ecosystem/[slug]/page.tsx"
      via: "Link href to /ecosystem/${slug}"
      pattern: "ecosystem.*slug"
---

<objective>
Create the /ecosystem (with /ecosystem/[slug] product pages) and /join static pages.

Purpose: /ecosystem provides the full Layer 0/1/2 architecture view with clickable product cards. /join expands the home CTA section into a dedicated contribution page with working external links.
Output: Static product data file, three new page routes, and i18n content for all three locales.
</objective>

<execution_context>
@/Users/ekitcho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ekitcho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-static-pages/14-CONTEXT.md
@.planning/phases/14-static-pages/14-RESEARCH.md

<interfaces>
<!-- Layout wrappers from Phase 9 -->

From src/components/layout/ProseLayout.tsx:
```typescript
export default function ProseLayout({ children, className = '' }: ProseLayoutProps)
// max-w-prose mx-auto px-4 sm:px-6 lg:px-8
```

From src/components/layout/GridLayout.tsx:
```typescript
export default function GridLayout({ children, className = '' }: GridLayoutProps)
// max-w-layout mx-auto px-4 sm:px-6 lg:px-8
```

From src/components/publications/PillTag.tsx:
```typescript
interface PillTagProps {
  label: string;
  layer?: number | null;
  active?: boolean;
}
export function PillTag({ label, layer, active }: PillTagProps)
// Layer styles: 0 = violet, 1 = teal, 2 = orange
```

From src/components/sections/Ecosystem.tsx (home section pattern):
```typescript
// Uses layers array: [{ key: 'layer0', icon: Shield, color: 'bg-layer-0' }, ...]
// 3-col grid of layer cards with icon, name, role, description
// framer-motion whileInView animations
```

From src/components/sections/CTA.tsx (home section pattern):
```typescript
// Uses actions array: [{ key: 'publish', icon: BookOpen, href: 'mailto:...' }, ...]
// 3-col grid of CTA cards with icon, title, description, link
// Links: mailto:contact@turfu.org, https://github.com/TURFu-org, https://discord.gg/turfu
```

From src/i18n.ts:
```typescript
export const locales = ['fr', 'en', 'tr'] as const;
```

Existing i18n namespaces (DO NOT modify):
- "ecosystem" — used by home Ecosystem section
- "cta" — used by home CTA section
New namespaces to create: "ecosystemPage", "joinPage"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create product data file and i18n content for ecosystem and join pages</name>
  <files>src/data/products.ts, src/messages/fr.json, src/messages/en.json, src/messages/tr.json</files>
  <read_first>
    - src/messages/fr.json
    - src/messages/en.json
    - src/messages/tr.json
    - src/components/sections/Ecosystem.tsx (for layer naming pattern)
    - src/components/sections/CTA.tsx (for CTA pattern)
    - .planning/sources/TURFu-Site-Livrable-v0.3.md (lines 318-347 for ecosystem content, lines 336-347 for CTA content)
    - .planning/phases/14-static-pages/14-RESEARCH.md (for product data structure)
  </read_first>
  <action>
**Create src/data/products.ts** (create the `src/data/` directory first):

```typescript
export interface Product {
  slug: string;
  layer: 0 | 1 | 2;
  status: 'concept' | 'development' | 'beta' | 'live';
  externalUrl?: string;
  stack?: string[];
}

export const products: Product[] = [
  { slug: 'turfu-org', layer: 0, status: 'live', externalUrl: 'https://turfu.org' },
  { slug: 'epis-protocol', layer: 1, status: 'development', stack: ['Solidity', 'Hive', 'Node.js'] },
  { slug: 'pickr', layer: 2, status: 'development', externalUrl: 'https://pickr.epis.network', stack: ['Next.js', 'EPIS SDK', 'Chrome Extension'] },
  { slug: 'memo', layer: 2, status: 'concept', stack: ['Next.js', 'EPIS SDK'] },
  { slug: 'turfu-labs', layer: 2, status: 'live', externalUrl: 'https://github.com/TURFu-org', stack: ['Research', 'Open Science'] },
  { slug: 'tcp', layer: 2, status: 'development', stack: ['AI Agents', 'LLM Pipeline'] },
  { slug: 'turfurxiv', layer: 2, status: 'concept' },
];

export function getProductBySlug(slug: string): Product | undefined {
  return products.find((p) => p.slug === slug);
}

export function getProductsByLayer(layer: 0 | 1 | 2): Product[] {
  return products.filter((p) => p.layer === layer);
}
```

**Add i18n content to fr.json** — add two new top-level namespaces `ecosystemPage` and `joinPage`:

```json
"ecosystemPage": {
  "title": "L'ecosysteme TURFu",
  "metaDescription": "Architecture Layer 0/1/2 de TURFu : recherche, infrastructure et applications pour la coordination epistemique.",
  "subtitle": "TURFu opere sur trois niveaux. Un cadre de recherche produit les fondations. Un incubateur les transforme en infrastructure. Des applications concretes les mettent entre les mains des utilisateurs.",
  "layers": {
    "0": "Layer 0 — Recherche",
    "1": "Layer 1 — Infrastructure",
    "2": "Layer 2 — Applications"
  },
  "layerDescriptions": {
    "0": "Cadre metaethique, recherche transdisciplinaire, publications open science. Continuite du CIRET avec les outils du XXIe siecle.",
    "1": "Incubateur et lab. Protocole EPIS (infrastructure epistemique on-chain), SDK, pipelines IA, standards.",
    "2": "Des outils concrets pour la recherche ouverte, la curation collaborative et le traitement de contenu."
  },
  "status": {
    "concept": "Concept",
    "development": "En developpement",
    "beta": "Beta",
    "live": "En production"
  },
  "productDetail": {
    "problem": "Probleme",
    "solution": "Solution",
    "status": "Statut",
    "stack": "Stack technique",
    "visit": "Visiter",
    "backToEcosystem": "Retour a l'ecosysteme"
  },
  "products": {
    "turfu-org": {
      "name": "TURFu ORG",
      "tagline": "Centre de recherche transdisciplinaire open science",
      "problem": "Les institutions de recherche traditionnelles sont cloisonnees par discipline et peu accessibles au public.",
      "solution": "Un centre de recherche ouvert qui publie analyses, audits et specifications en acces libre, heritier du CIRET avec les outils du XXIe siecle."
    },
    "epis-protocol": {
      "name": "Protocole EPIS",
      "tagline": "Infrastructure epistemique on-chain",
      "problem": "La validation du savoir repose sur des systemes opaques, lents et manipulables (peer-review traditionnel, reputation informelle).",
      "solution": "Un protocole decentralise ou chaque review, vote et challenge est verifiable, transparent et resistant a la manipulation. Infrastructure pour des reseaux sociaux epistemiques."
    },
    "pickr": {
      "name": "PICKR",
      "tagline": "Reviews epistemiques d'URL",
      "problem": "Le contenu en ligne manque d'evaluation structuree — les commentaires sociaux ne distinguent pas opinion et analyse.",
      "solution": "N'importe quelle URL du web peut etre soumise a l'analyse d'agents IA specialises, avec des resultats visibles a cote du commentaire social humain."
    },
    "memo": {
      "name": "MEMO",
      "tagline": "Curation collaborative",
      "problem": "La curation de contenu est fragmentee entre plateformes et manque de methode epistemique.",
      "solution": "Un outil de curation collaborative ancre sur le protocole EPIS, permettant de collecter, annoter et partager du contenu avec rigueur."
    },
    "turfu-labs": {
      "name": "TURFu Labs",
      "tagline": "R&D civilisationnelle open source",
      "problem": "La R&D sur les problemes civilisationnels est dispersee et peu coordonnee.",
      "solution": "Programmes de recherche-action ouverts couvrant gouvernance, coordination, epistemologie appliquee. Code et resultats publics."
    },
    "tcp": {
      "name": "TCP",
      "tagline": "Traitement de contenu par pipeline IA",
      "problem": "Le volume de contenu depasse la capacite humaine d'analyse et de categorisation.",
      "solution": "Pipeline d'agents IA specialises pour analyser, categoriser et enrichir du contenu selon des criteres epistemiques."
    },
    "turfurxiv": {
      "name": "TURFurxiv",
      "tagline": "Preprints open science",
      "problem": "Les preprints manquent d'un systeme de review transparent et decentralise.",
      "solution": "Plateforme de preprints ancree sur le protocole EPIS, combinant publication ouverte et review structuree."
    }
  }
}
```

```json
"joinPage": {
  "title": "Rejoindre TURFu",
  "metaDescription": "Contribuez a TURFu : publiez, construisez ou rejoignez la communaute de recherche transdisciplinaire.",
  "subtitle": "TURFu est un projet ouvert. Que vous soyez chercheur, developpeur, philosophe ou simplement curieux — il y a une place pour contribuer.",
  "publish_title": "Publier",
  "publish_desc": "Proposez un article, une analyse, un audit de protocole ou un document de recherche. Notre corpus est public, versionne et ouvert a la critique. Nous recherchons des contributions transdisciplinaires qui croisent epistemologie, ethique, IA et technologies decentralisees.",
  "publish_cta": "Proposer une publication",
  "build_title": "Construire",
  "build_desc": "Contribuez au code, aux outils, au protocole EPIS. Nos repos sont open source et nous accueillons les contributions techniques — du frontend au smart contract, des pipelines IA aux SDK.",
  "build_cta": "GitHub TURFu",
  "join_title": "Rejoindre",
  "join_desc": "Participez aux discussions, a la reflexion collective et aux debats structures. Notre Discord est l'espace ou la communaute TURFu se coordonne au quotidien.",
  "join_cta": "Discord TURFu",
  "contact_label": "Autre question ?",
  "contact_desc": "Ecrivez-nous directement.",
  "contact_cta": "contact@turfu.org"
}
```

Translate for EN (reasonable English). For TR, use FR as placeholder (Phase 16 handles full i18n).

IMPORTANT: Do NOT modify existing `ecosystem` or `cta` namespaces — those are used by the home page sections.
  </action>
  <verify>
    <automated>cd /Users/ekitcho/Desktop/dev-claude-lab/turfu-org-landing && node -e "const fr=require('./src/messages/fr.json'); const p=require('./src/data/products'); if(!fr.ecosystemPage||!fr.joinPage) throw new Error('missing namespaces'); if(p.products.length!==7) throw new Error('expected 7 products, got '+p.products.length); if(!p.getProductBySlug('pickr')) throw new Error('getProductBySlug failed'); console.log('OK: ecosystemPage products:', Object.keys(fr.ecosystemPage.products).length, 'joinPage keys:', Object.keys(fr.joinPage).length, 'products.ts:', p.products.length);"</automated>
  </verify>
  <acceptance_criteria>
    - src/data/products.ts exports Product type, products array (7 items), getProductBySlug, getProductsByLayer
    - products array has: turfu-org (L0), epis-protocol (L1), pickr/memo/turfu-labs/tcp/turfurxiv (L2)
    - fr.json contains `"ecosystemPage"` with layers, layerDescriptions, status, productDetail, and products sub-objects
    - fr.json contains `"joinPage"` with title, subtitle, 3 contribution sections, contact section
    - en.json and tr.json contain both namespaces
    - Existing `"ecosystem"` and `"cta"` namespaces in fr.json are UNCHANGED
  </acceptance_criteria>
  <done>Product data file exists with 7 typed products. All locale files have ecosystemPage and joinPage namespaces with complete content.</done>
</task>

<task type="auto">
  <name>Task 2: Create /ecosystem, /ecosystem/[slug], and /join page routes</name>
  <files>src/app/[locale]/ecosystem/page.tsx, src/app/[locale]/ecosystem/[slug]/page.tsx, src/app/[locale]/join/page.tsx</files>
  <read_first>
    - src/data/products.ts (just created in Task 1)
    - src/messages/fr.json (for ecosystemPage and joinPage keys)
    - src/app/[locale]/publications/page.tsx (page pattern)
    - src/app/[locale]/publications/[slug]/page.tsx (dynamic route pattern)
    - src/components/layout/ProseLayout.tsx
    - src/components/layout/GridLayout.tsx
    - src/components/publications/PillTag.tsx
    - src/components/sections/Ecosystem.tsx (visual reference for layer cards)
    - src/components/sections/CTA.tsx (visual reference for CTA cards)
    - src/i18n.ts (locales array for generateStaticParams)
  </read_first>
  <action>
**Create src/app/[locale]/ecosystem/page.tsx:**

Server component using GridLayout. Structure:
1. H1 title + subtitle paragraph
2. Layer overview section: 3-column grid (`grid md:grid-cols-3 gap-6 mb-16`) with layer cards for L0, L1, L2
   - Each layer card: `relative p-8 rounded-2xl bg-paper border border-border overflow-hidden`
   - Layer-colored top stripe: `absolute top-0 left-0 right-0 h-1 bg-layer-{n}` (use template literal with layer number)
   - Layer number watermark: `absolute top-4 right-4 text-6xl font-bold text-ink/5`
   - Layer name from `t('layers.{n}')`, description from `t('layerDescriptions.{n}')`
   - Import `Shield, Rocket, Boxes` from lucide-react for L0/L1/L2 icons (same as home Ecosystem section)
3. Product grid section: Group products by layer. For each layer group (L2 first, then L1, then L0):
   - Layer section heading with PillTag
   - Products in `grid md:grid-cols-2 lg:grid-cols-3 gap-6`
   - Each product card is a `Link` to `/${locale}/ecosystem/${product.slug}`
   - Card: `group relative p-6 rounded-2xl bg-paper border border-border overflow-hidden hover:border-accent transition-colors`
   - Top accent stripe: `absolute top-0 left-0 right-0 h-1 bg-layer-{product.layer}`
   - Product name from `t('products.${slug}.name')` in `text-xl font-bold mt-3 mb-2 group-hover:text-accent transition-colors`
   - Tagline from `t('products.${slug}.tagline')` in `text-ink-secondary text-body leading-relaxed`
   - Status badge from `t('status.${product.status}')` in small muted text

Import `products, getProductsByLayer` from `@/data/products`.
Import `Link` from `next/link`.
Import `PillTag` from `@/components/publications/PillTag`.

Export `generateMetadata` with title and description.

Do NOT use `'use client'` or framer-motion — keep as server component.
Do NOT use `export const dynamic` — this is a static page.

**Create src/app/[locale]/ecosystem/[slug]/page.tsx:**

Server component using ProseLayout. Product detail page:

Props: `{ params: { locale: string; slug: string } }`

1. `setRequestLocale(locale)` first
2. `getProductBySlug(slug)` — if not found, call `notFound()`
3. `getTranslations('ecosystemPage')` for content
4. Back link to `/ecosystem` at top
5. PillTag with layer label
6. H1 product name
7. Tagline as subtitle paragraph
8. Structured sections:
   - "Probleme" (H2) + `t('products.${slug}.problem')`
   - "Solution" (H2) + `t('products.${slug}.solution')`
   - "Statut" (H2) + status badge using `t('status.${product.status}')`
   - "Stack technique" (H2) + pills for each item in `product.stack` (only if stack exists)
   - External link button (only if `product.externalUrl` exists): `t('productDetail.visit')` as an `<a>` with `target="_blank"`

Export `generateStaticParams` that returns all product slugs crossed with all locales:
```typescript
import { products } from '@/data/products';
import { locales } from '@/i18n';

export function generateStaticParams() {
  return products.flatMap((product) =>
    locales.map((locale) => ({
      locale,
      slug: product.slug,
    }))
  );
}
```

Export `generateMetadata` using product name and tagline.

**Create src/app/[locale]/join/page.tsx:**

Server component using ProseLayout. Structure:

1. H1 title in `font-display text-4xl md:text-5xl text-ink leading-tight mb-4`
2. Subtitle paragraph in `text-lg text-ink-secondary leading-relaxed mb-12`
3. Three contribution cards in `grid gap-8 mb-16`:
   - Each card: `p-8 rounded-2xl bg-paper-warm border border-border`
   - Icon from lucide-react: BookOpen (publish), Hammer (build), Users (join) — same as CTA.tsx
   - H2 title in `font-display text-2xl text-ink mb-3`
   - Description in `text-body text-ink-secondary leading-relaxed mb-6` (2-3 sentences from joinPage i18n)
   - CTA link as `<a>` with `inline-flex items-center gap-2 text-accent hover:text-accent-hover font-medium transition-colors`
   - Links: publish = `mailto:contact@turfu.org`, build = `https://github.com/TURFu-org`, join = `https://discord.gg/turfu`
   - External links get `target="_blank" rel="noopener noreferrer"`
4. Contact section at bottom: small centered text with mailto link

Import `ArrowRight` from lucide-react for CTA arrow icons.

Export `generateMetadata` with title and description.

Do NOT use `'use client'` — keep as server component. No framer-motion needed.
  </action>
  <verify>
    <automated>cd /Users/ekitcho/Desktop/dev-claude-lab/turfu-org-landing && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/[locale]/ecosystem/page.tsx exists and contains: `setRequestLocale`, `getTranslations('ecosystemPage')`, `getProductsByLayer`, `Link`, `PillTag`, `bg-layer-`, `generateMetadata`
    - src/app/[locale]/ecosystem/[slug]/page.tsx exists and contains: `setRequestLocale`, `getProductBySlug`, `notFound`, `generateStaticParams`, `locales`, `ProseLayout`, `generateMetadata`
    - src/app/[locale]/join/page.tsx exists and contains: `setRequestLocale`, `getTranslations('joinPage')`, `ProseLayout`, `mailto:contact@turfu.org`, `https://github.com/TURFu-org`, `https://discord.gg/turfu`, `generateMetadata`
    - None of the pages use `'use client'` or `export const dynamic`
    - TypeScript compilation passes with no errors
  </acceptance_criteria>
  <done>/ecosystem shows layer overview and clickable product cards. /ecosystem/[slug] shows product details with problem/solution/status/stack. /join shows 3 contribution CTAs with working external links. All pages have metadata. TypeScript passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (no type errors)
2. `npm run build` succeeds (all pages statically generate, including all 7 ecosystem/[slug] pages)
3. products.ts exports 7 products across 3 layers
4. ecosystem/page.tsx renders layer cards and product grid with layer-colored accents
5. ecosystem/[slug]/page.tsx has generateStaticParams covering all 7 slugs x 3 locales
6. join/page.tsx has 3 CTA cards with correct external links
7. fr.json ecosystemPage namespace has products.*.name/tagline/problem/solution for all 7 products
8. fr.json joinPage namespace has 3 contribution sections with detailed descriptions
</verification>

<success_criteria>
- /ecosystem displays Layer 0/1/2 schema with 7 product cards featuring layer-colored accent stripes
- Clicking any product card navigates to /ecosystem/[slug] with problem/solution/status/stack detail
- /join displays 3 contribution paths (publish/build/join) with working links to Discord, GitHub, and email
- All pages are server components with correct generateMetadata
- Build passes with all ecosystem/[slug] pages pre-rendered via generateStaticParams
</success_criteria>

<output>
After completion, create `.planning/phases/14-static-pages/14-02-SUMMARY.md`
</output>
