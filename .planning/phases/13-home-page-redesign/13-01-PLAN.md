---
phase: 13-home-page-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/[locale]/page.tsx
  - src/components/sections/Hero.tsx
  - src/components/sections/LatestPublications.tsx
  - src/messages/fr.json
  - src/messages/en.json
  - src/messages/tr.json
autonomous: true
requirements: [HOME-01, HOME-02, HOME-05]

must_haves:
  truths:
    - "Hero displays the livrable H1 in Instrument Serif with two working CTAs"
    - "Latest publications section shows 3 cards from database when publications exist"
    - "Latest publications section is hidden when 0 publications exist"
    - "Old sections (Problem, Vision, Architecture, Principles, ScrollSpy) are removed from home page"
  artifacts:
    - path: "src/components/sections/Hero.tsx"
      provides: "Reworked hero with livrable H1, subtitle, 2 CTAs"
      contains: "Lire nos publications"
    - path: "src/components/sections/LatestPublications.tsx"
      provides: "3-card publications grid with scroll animation"
      contains: "PublicationCard"
    - path: "src/app/[locale]/page.tsx"
      provides: "Async server component with publications fetch"
      contains: "getPublishedPublications"
  key_links:
    - from: "src/app/[locale]/page.tsx"
      to: "src/lib/publications.ts"
      via: "getPublishedPublications({ locale, limit: 3 })"
      pattern: "getPublishedPublications"
    - from: "src/app/[locale]/page.tsx"
      to: "src/components/sections/LatestPublications.tsx"
      via: "publications prop passed from server to client"
      pattern: "publications={publications}"
    - from: "src/components/sections/LatestPublications.tsx"
      to: "src/components/publications/PublicationCard.tsx"
      via: "direct import and render"
      pattern: "PublicationCard"
---

<objective>
Rebuild the home page hero and latest publications sections, rewire page.tsx as an async server component, and remove old one-pager sections.

Purpose: Transform the home page from a 7-section manifesto-style one-pager into an editorial landing with content-driven sections.
Output: Working hero with livrable copy, latest publications from database, old sections removed.
</objective>

<execution_context>
@/Users/ekitcho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ekitcho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-home-page-redesign/13-CONTEXT.md
@.planning/phases/13-home-page-redesign/13-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/types.ts:
```typescript
export interface PublicationMeta {
  id: string;
  slug: string;
  locale: string;
  title: string;
  abstract: string | null;
  author: string | null;
  discipline: string | null;
  type: string | null;
  layer: number | null;
  tags: string[];
  featured_image: string | null;
  published_at: string | null;
}
```

From src/lib/publications.ts:
```typescript
export async function getPublishedPublications(opts: {
  locale: string;
  discipline?: string;
  tag?: string;
  page?: number;
  limit?: number;
}): Promise<{ publications: PublicationMeta[]; total: number }>
```

From src/components/publications/PublicationCard.tsx:
```typescript
interface PublicationCardProps {
  publication: PublicationMeta;
  locale: string;
}
export function PublicationCard({ publication, locale }: PublicationCardProps)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework Hero and create LatestPublications component</name>
  <files>
    src/components/sections/Hero.tsx,
    src/components/sections/LatestPublications.tsx
  </files>
  <read_first>
    src/components/sections/Hero.tsx,
    src/components/publications/PublicationCard.tsx,
    src/lib/types.ts,
    .planning/sources/TURFu-Site-Livrable-v0.3.md
  </read_first>
  <action>
    **Hero.tsx** — Rework in place (keep 'use client', useTranslations('hero'), framer-motion pattern):
    - H1: Use `t('tagline')` — will contain livrable text "Un centre de recherche ouvert pour repenser comment l'humanite valide, partage et gouverne le savoir." Render in `font-display` (Instrument Serif). Keep existing size classes: `text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-display font-bold`
    - Remove the `text-accent` span wrapping the tagline — render H1 in standard `text-ink` color for editorial feel
    - Subtitle: Use `t('subtitle')` — full livrable paragraph. Keep existing classes: `text-lg sm:text-xl text-ink-secondary max-w-2xl mx-auto mb-12 leading-relaxed`. Remove the separate `description` paragraph — merge subtitle into one paragraph
    - CTA primary: `<Link href={\`/\${locale}/publications\`}>` with `t('cta_primary')`. Use `next/link` Link component. Need to get locale — add `useLocale()` from next-intl. Keep existing button styling (bg-accent, text-white). Replace FileText icon with BookOpen from lucide-react
    - CTA secondary: `<Link href={\`/\${locale}/vision\`}>` with `t('cta_secondary')`. Keep border button styling. Keep ArrowRight icon
    - Keep: min-h-screen, gradient background, scroll-down bounce indicator, fade+slide-up animation
    - Remove: `<a href="#">` and `<a href="#cta">` — replace with Link components as above
    - Add import: `import Link from 'next/link'` and `import { useLocale } from 'next-intl'`
    - Add `const locale = useLocale();` inside the component

    **LatestPublications.tsx** — Create new file:
    - 'use client' component
    - Props: `{ publications: PublicationMeta[]; locale: string }`
    - Import `useTranslations` from next-intl, namespace 'latestPublications'
    - Import `motion` from framer-motion
    - Import `PublicationCard` from '@/components/publications/PublicationCard'
    - Import `Link` from 'next/link' and `ArrowRight` from 'lucide-react'
    - Section with `className="py-20 bg-paper-warm"`
    - Container: `max-w-layout mx-auto px-4 sm:px-6 lg:px-8`
    - H2 with `t('title')` in `text-3xl md:text-4xl font-display text-center mb-4` with whileInView animation
    - Subtitle paragraph with `t('subtitle')` in `text-ink-secondary text-lg text-center mb-12`
    - 3-column grid: `grid md:grid-cols-3 gap-6`
    - Map over publications, wrap each PublicationCard in motion.div with staggered delay (index * 0.1)
    - "View all" link at bottom: `Link href={\`/\${locale}/publications\`}` with `t('viewAll')` text and ArrowRight icon
    - All motion elements use `whileInView`, `viewport={{ once: true }}`, `initial={{ opacity: 0, y: 20 }}`
  </action>
  <verify>
    <automated>grep -n "getPublishedPublications\|PublicationCard\|useLocale\|latestPublications\|font-display" src/components/sections/Hero.tsx src/components/sections/LatestPublications.tsx 2>/dev/null | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Hero.tsx contains `import Link from 'next/link'`
    - Hero.tsx contains `useLocale` import from next-intl
    - Hero.tsx contains `href={` with `/publications` path
    - Hero.tsx contains `href={` with `/vision` path
    - Hero.tsx does NOT contain `<a href="#"`
    - Hero.tsx does NOT contain `t('description')` (merged into subtitle)
    - LatestPublications.tsx contains `'use client'`
    - LatestPublications.tsx contains `useTranslations('latestPublications')`
    - LatestPublications.tsx contains `PublicationCard`
    - LatestPublications.tsx contains `publications: PublicationMeta[]`
    - LatestPublications.tsx contains `bg-paper-warm`
    - LatestPublications.tsx contains `md:grid-cols-3`
  </acceptance_criteria>
  <done>Hero displays livrable H1 with two Link CTAs to /publications and /vision. LatestPublications renders a 3-card grid using PublicationCard with scroll animations.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire page.tsx and update i18n for hero + publications</name>
  <files>
    src/app/[locale]/page.tsx,
    src/messages/fr.json,
    src/messages/en.json,
    src/messages/tr.json
  </files>
  <read_first>
    src/app/[locale]/page.tsx,
    src/messages/fr.json,
    src/messages/en.json,
    src/messages/tr.json,
    src/lib/publications.ts,
    src/components/sections/Hero.tsx,
    src/components/sections/LatestPublications.tsx
  </read_first>
  <action>
    **page.tsx** — Rewrite as async server component:
    ```typescript
    import { setRequestLocale } from 'next-intl/server';
    import { getPublishedPublications } from '@/lib/publications';
    import Hero from '@/components/sections/Hero';
    import LatestPublications from '@/components/sections/LatestPublications';
    import Ecosystem from '@/components/sections/Ecosystem';
    import CTA from '@/components/sections/CTA';

    type Props = {
      params: { locale: string };
    };

    export default async function Home({ params: { locale } }: Props) {
      setRequestLocale(locale);

      const { publications } = await getPublishedPublications({
        locale,
        limit: 3,
      });

      return (
        <>
          <Hero />
          {publications.length > 0 && (
            <LatestPublications publications={publications} locale={locale} />
          )}
          <Ecosystem />
          <CTA />
        </>
      );
    }
    ```
    - Remove imports: Problem, Vision, Architecture, Principles, ScrollSpy
    - Add imports: LatestPublications, getPublishedPublications
    - Make function `async`
    - Fetch publications with `limit: 3`
    - Conditionally render LatestPublications only when publications.length > 0
    - Keep Ecosystem and CTA imports (reworked in Plan 02)

    **Delete old files** (git rm):
    - `src/components/sections/Problem.tsx`
    - `src/components/sections/Vision.tsx`
    - `src/components/sections/Architecture.tsx`
    - `src/components/sections/Principles.tsx`
    - `src/components/ScrollSpy.tsx`
    - `src/hooks/useScrollSpy.ts`

    **fr.json** — Update hero keys and add latestPublications namespace:
    - Replace `"hero"` section with:
      ```json
      "hero": {
        "tagline": "Un centre de recherche ouvert pour repenser comment l'humanite valide, partage et gouverne le savoir.",
        "subtitle": "TURFu est un projet de recherche transdisciplinaire. Nous produisons des analyses, construisons des outils et publions en open science — a l'intersection de l'epistemologie, de l'ethique, de l'IA et des technologies decentralisees.",
        "cta_primary": "Lire nos publications",
        "cta_secondary": "Decouvrir la vision"
      }
      ```
    - Add new namespace after hero:
      ```json
      "latestPublications": {
        "title": "Publications recentes",
        "subtitle": "Analyses, audits, specifications — notre recherche est publique.",
        "viewAll": "Toutes les publications"
      }
      ```
    - Remove entire namespaces: `"problem"`, `"vision"`, `"architecture"`, `"principles"`

    **en.json** — Same structure:
    - Replace `"hero"` with:
      ```json
      "hero": {
        "tagline": "An open research center rethinking how humanity validates, shares, and governs knowledge.",
        "subtitle": "TURFu is a transdisciplinary research project. We produce analyses, build tools, and publish in open science — at the intersection of epistemology, ethics, AI, and decentralized technologies.",
        "cta_primary": "Read our publications",
        "cta_secondary": "Discover the vision"
      }
      ```
    - Add:
      ```json
      "latestPublications": {
        "title": "Recent publications",
        "subtitle": "Analyses, audits, specifications — our research is public.",
        "viewAll": "All publications"
      }
      ```
    - Remove: `"problem"`, `"vision"`, `"architecture"`, `"principles"`

    **tr.json** — Same structure:
    - Replace `"hero"` with:
      ```json
      "hero": {
        "tagline": "Insanligin bilgiyi nasil dogruladigini, paylasitigini ve yonettigini yeniden dusunmek icin acik bir arastirma merkezi.",
        "subtitle": "TURFu disiplinlerarasi bir arastirma projesidir. Analizler uretiyor, araclar insa ediyor ve acik bilim olarak yayinliyoruz — epistemoloji, etik, yapay zeka ve merkezi olmayan teknolojilerin kesisiminde.",
        "cta_primary": "Yayinlarimizi okuyun",
        "cta_secondary": "Vizyonu kesfedin"
      }
      ```
    - Add:
      ```json
      "latestPublications": {
        "title": "Son yayinlar",
        "subtitle": "Analizler, denetimler, spesifikasyonlar — arastirmamiz halka aciktir.",
        "viewAll": "Tum yayinlar"
      }
      ```
    - Remove: `"problem"`, `"vision"`, `"architecture"`, `"principles"`
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - page.tsx contains `async function Home`
    - page.tsx contains `getPublishedPublications`
    - page.tsx contains `publications.length > 0`
    - page.tsx does NOT contain `ScrollSpy`
    - page.tsx does NOT contain `Problem`
    - page.tsx does NOT contain `Vision` (as import)
    - page.tsx does NOT contain `Architecture`
    - page.tsx does NOT contain `Principles`
    - src/components/sections/Problem.tsx does NOT exist
    - src/components/sections/Vision.tsx does NOT exist
    - src/components/sections/Architecture.tsx does NOT exist
    - src/components/sections/Principles.tsx does NOT exist
    - src/components/ScrollSpy.tsx does NOT exist
    - src/hooks/useScrollSpy.ts does NOT exist
    - fr.json contains `"latestPublications"`
    - fr.json does NOT contain `"problem"`
    - en.json contains `"latestPublications"`
    - en.json does NOT contain `"problem"`
    - tr.json contains `"latestPublications"`
    - tr.json does NOT contain `"problem"`
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>page.tsx is an async server component fetching 3 latest publications, rendering Hero + conditional LatestPublications + Ecosystem + CTA. Old section files deleted. All 3 locale files updated with new hero copy and latestPublications namespace, old namespaces removed. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds with no errors
- `grep -r "Problem\|ScrollSpy\|Architecture\|Principles" src/app/[locale]/page.tsx` returns nothing
- `grep "getPublishedPublications" src/app/[locale]/page.tsx` returns a match
- `grep "latestPublications" src/messages/fr.json src/messages/en.json src/messages/tr.json` returns matches in all 3 files
- `ls src/components/sections/Problem.tsx 2>/dev/null` returns nothing (file deleted)
</verification>

<success_criteria>
- Home page renders hero with livrable H1 text and two functional Link CTAs
- LatestPublications section renders 3 publication cards when publications exist in database
- LatestPublications section is completely hidden when 0 publications exist
- Old sections (Problem, Vision, Architecture, Principles) are removed from page and files deleted
- ScrollSpy component and hook deleted
- All 3 locale files have updated hero keys and new latestPublications namespace
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/13-home-page-redesign/13-01-SUMMARY.md`
</output>
