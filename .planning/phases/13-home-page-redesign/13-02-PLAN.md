---
phase: 13-home-page-redesign
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified:
  - src/components/sections/Ecosystem.tsx
  - src/components/sections/CTA.tsx
  - src/messages/fr.json
  - src/messages/en.json
  - src/messages/tr.json
autonomous: true
requirements: [HOME-03, HOME-04]

must_haves:
  truths:
    - "Ecosystem section shows H2 'Ce qu'on construit' with intro paragraph and 3 layer cards"
    - "CTA section shows H2 'Contribuer' with 3 cards linking to Discord, GitHub, and mailto"
    - "Page is fully responsive down to 375px mobile"
  artifacts:
    - path: "src/components/sections/Ecosystem.tsx"
      provides: "Reworked ecosystem preview with livrable descriptions"
      contains: "Ce qu'on construit"
    - path: "src/components/sections/CTA.tsx"
      provides: "Reworked contribution CTA with 3 cards and external links"
      contains: "discord"
  key_links:
    - from: "src/components/sections/Ecosystem.tsx"
      to: "src/messages/fr.json"
      via: "useTranslations('ecosystem')"
      pattern: "useTranslations.*ecosystem"
    - from: "src/components/sections/CTA.tsx"
      to: "src/messages/fr.json"
      via: "useTranslations('cta')"
      pattern: "useTranslations.*cta"
---

<objective>
Rework the Ecosystem and CTA sections with livrable copy, updated descriptions, and correct external links.

Purpose: Complete the 4-section home page with ecosystem overview (Layer 0/1/2) and contribution CTA with working external links.
Output: Reworked Ecosystem.tsx and CTA.tsx with updated i18n keys matching livrable content.
</objective>

<execution_context>
@/Users/ekitcho/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ekitcho/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-home-page-redesign/13-CONTEXT.md
@.planning/phases/13-home-page-redesign/13-RESEARCH.md
@.planning/phases/13-home-page-redesign/13-01-SUMMARY.md

<interfaces>
<!-- Ecosystem.tsx current structure (to rework in place) -->
From src/components/sections/Ecosystem.tsx:
```typescript
'use client';
// Uses useTranslations('ecosystem'), framer-motion whileInView
// 3 layer cards: layer0 (Shield), layer1 (Rocket), layer2 (Boxes)
// bg-layer-0, bg-layer-1, bg-layer-2 color tokens
// max-w-layout container, md:grid-cols-3 grid
// Has layer flow indicator (L0—L1—L2 line) at bottom — to be removed
```

From src/components/sections/CTA.tsx:
```typescript
'use client';
// Uses useTranslations('cta'), framer-motion whileInView
// 3 profile cards: contributor (Discord), builder (#), partner (mailto)
// max-w-layout container, md:grid-cols-3 grid
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rework Ecosystem section with livrable content</name>
  <files>
    src/components/sections/Ecosystem.tsx,
    src/messages/fr.json,
    src/messages/en.json,
    src/messages/tr.json
  </files>
  <read_first>
    src/components/sections/Ecosystem.tsx,
    src/messages/fr.json,
    src/messages/en.json,
    src/messages/tr.json,
    .planning/sources/TURFu-Site-Livrable-v0.3.md
  </read_first>
  <action>
    **Ecosystem.tsx** — Rework in place:
    - Keep: 'use client', useTranslations('ecosystem'), framer-motion pattern, 3 layer cards with Shield/Rocket/Boxes icons, bg-layer-0/1/2 tokens, max-w-layout container
    - Add intro paragraph: After the H2 title, add a `<p>` with `t('intro')` — `text-lg text-ink-secondary text-center max-w-3xl mx-auto mb-16 leading-relaxed`. Wrap in motion.div with whileInView animation
    - Change H2 title: will use `t('title')` which will now be "Ce qu'on construit" (updated in i18n below)
    - Change section id from `id="ecosystem"` to no id (or keep for anchor fallback from hero CTA — keep `id="ecosystem"`)
    - Change section background: Replace `bg-paper-depth` with `bg-paper` (alternating rhythm: hero bg-paper, publications bg-paper-warm, ecosystem bg-paper, CTA bg-paper-warm)
    - Remove the layer flow indicator (lines 67-75 in current file: the `hidden md:flex` div with "Layer 0 — Layer 1 — Layer 2" text)
    - Keep card structure: rounded-2xl, border, hover effect, icon badge, name/role/description
    - Update layer card data to use the existing key pattern (`${layer.key}_name`, `${layer.key}_role`, `${layer.key}_desc`) — just updating i18n values

    **fr.json** — Update `"ecosystem"` namespace:
    ```json
    "ecosystem": {
      "title": "Ce qu'on construit",
      "intro": "TURFu opere sur trois niveaux. Un cadre de recherche (Layer 0) produit les fondations ethiques et epistemiques. Un incubateur (Layer 1) transforme ces fondations en infrastructure technique. Des applications concretes (Layer 2) mettent le tout entre les mains des utilisateurs.",
      "layer0_name": "TURFu ORG",
      "layer0_role": "Layer 0 — Recherche",
      "layer0_desc": "Cadre metaethique, recherche transdisciplinaire, publications open science. Continuite du CIRET avec les outils du XXIe siecle.",
      "layer1_name": "OZAM",
      "layer1_role": "Layer 1 — Incubateur",
      "layer1_desc": "Incubateur et lab. Protocole EPIS (infrastructure epistemique on-chain), SDK, pipelines IA, standards.",
      "layer2_name": "Produits",
      "layer2_role": "Layer 2 — Applications",
      "layer2_desc": "PICKR, MEMO, TURFu Labs, TCP, TURFurxiv — des outils concrets pour la recherche ouverte."
    }
    ```

    **en.json** — Update `"ecosystem"` namespace:
    ```json
    "ecosystem": {
      "title": "What we build",
      "intro": "TURFu operates on three levels. A research framework (Layer 0) produces ethical and epistemic foundations. An incubator (Layer 1) transforms those foundations into technical infrastructure. Concrete applications (Layer 2) put it all in users' hands.",
      "layer0_name": "TURFu ORG",
      "layer0_role": "Layer 0 — Research",
      "layer0_desc": "Metaethical framework, transdisciplinary research, open science publications. Continuing the CIRET with 21st century tools.",
      "layer1_name": "OZAM",
      "layer1_role": "Layer 1 — Incubator",
      "layer1_desc": "Incubator and lab. EPIS protocol (on-chain epistemic infrastructure), SDK, AI pipelines, standards.",
      "layer2_name": "Products",
      "layer2_role": "Layer 2 — Applications",
      "layer2_desc": "PICKR, MEMO, TURFu Labs, TCP, TURFurxiv — concrete tools for open research."
    }
    ```

    **tr.json** — Update `"ecosystem"` namespace:
    ```json
    "ecosystem": {
      "title": "Ne insa ediyoruz",
      "intro": "TURFu uc seviyede faaliyet gosteriyor. Bir arastirma cercevesi (Katman 0) etik ve epistemik temelleri uretiyor. Bir kulucka (Katman 1) bu temelleri teknik altyapiya donusturuyor. Somut uygulamalar (Katman 2) hepsini kullanicilarin eline veriyor.",
      "layer0_name": "TURFu ORG",
      "layer0_role": "Katman 0 — Arastirma",
      "layer0_desc": "Metaetik cerceve, disiplinlerarasi arastirma, acik bilim yayinlari. CIRET'in 21. yuzyil araclariyla devami.",
      "layer1_name": "OZAM",
      "layer1_role": "Katman 1 — Kulucka",
      "layer1_desc": "Kulucka ve lab. EPIS protokolu (zincir uzerinde epistemik altyapi), SDK, YZ islem hatlari, standartlar.",
      "layer2_name": "Urunler",
      "layer2_role": "Katman 2 — Uygulamalar",
      "layer2_desc": "PICKR, MEMO, TURFu Labs, TCP, TURFurxiv — acik arastirma icin somut araclar."
    }
    ```
  </action>
  <verify>
    <automated>grep -n "intro\|Ce qu'on construit\|What we build\|Layer 0\|hidden md:flex" src/components/sections/Ecosystem.tsx src/messages/fr.json src/messages/en.json 2>/dev/null | head -15</automated>
  </verify>
  <acceptance_criteria>
    - Ecosystem.tsx contains `t('intro')` rendering the intro paragraph
    - Ecosystem.tsx does NOT contain the layer flow indicator div (`hidden md:flex` with "Layer 0" spans and accent lines)
    - Ecosystem.tsx contains `bg-paper` (not `bg-paper-depth`) as section background
    - fr.json ecosystem.title is "Ce qu'on construit"
    - fr.json ecosystem contains `"intro"` key
    - fr.json ecosystem.layer0_desc contains "Cadre metaethique"
    - en.json ecosystem.title is "What we build"
    - en.json ecosystem contains `"intro"` key
    - tr.json ecosystem contains `"intro"` key
  </acceptance_criteria>
  <done>Ecosystem section displays H2 "Ce qu'on construit" with livrable intro paragraph and 3 layer cards with updated descriptions. Layer flow indicator removed. Section uses bg-paper background.</done>
</task>

<task type="auto">
  <name>Task 2: Rework CTA section with livrable copy and external links</name>
  <files>
    src/components/sections/CTA.tsx,
    src/messages/fr.json,
    src/messages/en.json,
    src/messages/tr.json
  </files>
  <read_first>
    src/components/sections/CTA.tsx,
    src/messages/fr.json,
    src/messages/en.json,
    src/messages/tr.json,
    .planning/sources/TURFu-Site-Livrable-v0.3.md
  </read_first>
  <action>
    **CTA.tsx** — Rework in place:
    - Keep: 'use client', useTranslations('cta'), framer-motion pattern, max-w-layout container, md:grid-cols-3 grid
    - Change section background: Replace `bg-paper-depth` with `bg-paper-warm` (rhythm: hero bg-paper, publications bg-paper-warm, ecosystem bg-paper, CTA bg-paper-warm)
    - Add intro paragraph: After H2, add `<p>` with `t('intro')` — `text-lg text-ink-secondary text-center max-w-3xl mx-auto mb-16 leading-relaxed`
    - Update the 3 profiles array to match livrable:
      ```typescript
      const actions = [
        { key: 'publish', icon: BookOpen, href: 'mailto:contact@turfu.org' },
        { key: 'build', icon: Hammer, href: 'https://github.com/TURFu-org' },
        { key: 'join', icon: Users, href: 'https://discord.gg/turfu' },
      ];
      ```
    - Import `BookOpen` from lucide-react (replace Handshake). Keep `Hammer` and `Users`
    - Update card rendering to use `actions` array with keys `publish`, `build`, `join` instead of old `contributor`, `builder`, `partner`
    - Card structure: icon badge (bg-accent), h3 with `t(action.key)`, description with `t(\`${action.key}_desc\`)`, link with `t(\`${action.key}_btn\`)`
    - External links: add `target="_blank"` and `rel="noopener noreferrer"` for Discord and GitHub URLs. mailto links don't need target
    - Remove card gradient (`bg-gradient-to-b from-paper-depth to-transparent`) — use `bg-paper border border-border rounded-2xl` for consistency with ecosystem cards

    **fr.json** — Update `"cta"` namespace:
    ```json
    "cta": {
      "title": "Contribuer",
      "intro": "TURFu est un projet ouvert. Que vous soyez chercheur, developpeur, philosophe ou simplement curieux — il y a une place pour contribuer.",
      "publish": "Publier",
      "publish_desc": "Proposez un article, une analyse ou un document de recherche.",
      "publish_btn": "Proposer",
      "build": "Construire",
      "build_desc": "Contribuez au code, aux outils, au protocole EPIS.",
      "build_btn": "GitHub",
      "join": "Rejoindre",
      "join_desc": "Participez aux discussions et a la reflexion collective.",
      "join_btn": "Discord"
    }
    ```

    **en.json** — Update `"cta"` namespace:
    ```json
    "cta": {
      "title": "Contribute",
      "intro": "TURFu is an open project. Whether you are a researcher, developer, philosopher, or simply curious — there is a place to contribute.",
      "publish": "Publish",
      "publish_desc": "Propose an article, an analysis, or a research document.",
      "publish_btn": "Propose",
      "build": "Build",
      "build_desc": "Contribute to the code, tools, and the EPIS protocol.",
      "build_btn": "GitHub",
      "join": "Join",
      "join_desc": "Participate in discussions and collective thinking.",
      "join_btn": "Discord"
    }
    ```

    **tr.json** — Update `"cta"` namespace:
    ```json
    "cta": {
      "title": "Katki saglayin",
      "intro": "TURFu acik bir projedir. Arastirmaci, gelistirici, filozof veya sadece merakli olun — katki saglamak icin bir yer var.",
      "publish": "Yayinla",
      "publish_desc": "Bir makale, analiz veya arastirma belgesi onerin.",
      "publish_btn": "Oner",
      "build": "Insa et",
      "build_desc": "Koda, araclara ve EPIS protokolune katki saglayin.",
      "build_btn": "GitHub",
      "join": "Katilin",
      "join_desc": "Tartismalara ve kolektif dusunceye katilin.",
      "join_btn": "Discord"
    }
    ```
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - CTA.tsx contains `mailto:contact@turfu.org` as href
    - CTA.tsx contains `https://github.com/TURFu-org` as href
    - CTA.tsx contains `https://discord.gg/turfu` as href
    - CTA.tsx contains `bg-paper-warm` as section background
    - CTA.tsx contains `t('intro')` rendering intro paragraph
    - CTA.tsx does NOT contain `contributor` key reference
    - CTA.tsx does NOT contain `partner` key reference
    - CTA.tsx does NOT contain `bg-gradient-to-b`
    - fr.json cta.title is "Contribuer"
    - fr.json cta contains `"intro"` key
    - fr.json cta contains `"publish"` key
    - fr.json does NOT contain `"contributor"` in cta namespace
    - en.json cta.title is "Contribute"
    - tr.json cta contains `"publish"` key
    - `npx next build` completes without errors
  </acceptance_criteria>
  <done>CTA section displays H2 "Contribuer" with intro paragraph and 3 cards (Publier/Construire/Rejoindre) linking to mailto, GitHub, and Discord. All i18n updated. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds with no errors
- `grep -c "intro" src/components/sections/Ecosystem.tsx src/components/sections/CTA.tsx` shows 1+ match in each
- `grep "discord\|github\|mailto" src/components/sections/CTA.tsx` returns all 3 links
- `grep "hidden md:flex" src/components/sections/Ecosystem.tsx` returns nothing (flow indicator removed)
- Home page renders 4 sections: Hero, LatestPublications (if data), Ecosystem, CTA
</verification>

<success_criteria>
- Ecosystem section shows "Ce qu'on construit" H2 with intro paragraph and 3 layer cards with livrable descriptions
- Layer flow indicator removed from Ecosystem section
- CTA section shows "Contribuer" H2 with intro paragraph and 3 action cards with working external links
- All 3 locale files have updated ecosystem and cta keys matching livrable copy
- Section background rhythm: hero bg-paper, publications bg-paper-warm, ecosystem bg-paper, CTA bg-paper-warm
- Page fully responsive (grid collapses to 1-column on mobile)
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/13-home-page-redesign/13-02-SUMMARY.md`
</output>
